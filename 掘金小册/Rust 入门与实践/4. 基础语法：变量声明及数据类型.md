上节中，我们上手体验了一下使用 Rust 写实用小工具，接下来，让我们开始学习 Rust 的基础知识，夯实基础吧！

这节里，我们将为大家介绍一下 Rust 中的变量的声明方式和数据类型。

Talk is cheap, show me the code，咱直接上代码！

我们以第二节的代码为例：

```Rust
use std::io;

fn main() {
    println!("Hello, world!");
    println!("Please input a number: ");
    let mut input = String::new(); // 这一行就是变量声明，let mut 代表声明的是一个可变的变量，如果不加 mut 默认声明的就是一个不可变的变量
    io::stdin()
        .read_line(&mut input) // 这里我们拿到了 input 这个变量的可变引用，也因此在声明的时候需要声明称可变变量，关于引用我们会在后续章节中详细介绍
        .expect("Failed to read input!");
    println!("Your raw input is: {:?}.", input);
    let number: i64 = input.trim().parse().expect("Input is not a number!"); // 这里的 number 是不可变的，类型我们指定为了 i64，在这里指定的原因是为了告诉编译器 parse 需要解析成什么类型，因为 String 可以 parse 成多个类型，所以在这里 Rust 编译器无法自行推断出来
    println!("Your input is: {}.", number);
}
```

我们可以试试看，把`let mut input`的 mut 去掉，会发现编译直接报错：

```Bash
$ cargo check
    Checking chapter-03 v0.1.0
error[E0596]: cannot borrow `input` as mutable, as it is not declared as mutable
 --> src/main.rs:8:20
  |
8 |         .read_line(&mut input)
  |                    ^^^^^^^^^^ cannot borrow as mutable
  |
help: consider changing this to be mutable
  |
6 |     let mut input = String::new();
  |         +++

For more information about this error, try `rustc --explain E0596`.
error: could not compile `chapter-03` due to previous error
```

编译器也非常贴心地提示了错误的原因以及如何修复。

好，再让我们加入一些新的代码：

```Rust
use std::io;

fn main() {
    println!("Hello, world!");
    println!("Please input a number: ");
    let mut input = String::new();
    io::stdin()
        .read_line(&mut input)
        .expect("Failed to read input!");
    println!("Your raw input is: {:?}.", input);
    let number: i64 = input.trim().parse().expect("Input is not a number!");
    println!("Your input is: {}.", number);

    let a = (1, 2);
    println!("a: {:?}", a);
    let (a, b) = a;
    println!("a: {:?}, b: {:}", a, b);
}
```

第 14 行我们用到了一个新的类型——元组（tuple），一个元组可以有任意多个成员。

第 16 行我们用到了元组的一个特性（本质是语法糖）——解构，以及 Rust 的一个特性——变量名可复用，这个特性是非常实用的，这样就不用想破脑袋想名字了，~~毕竟软件工程的一大难题就是起名~~。

<p align=center><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/8dcf1fd4c7054ab18bb9db9b71d3275a~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1080&h=958&e=jpg&b=1760b4" alt=""  width="30%"/></p>

接下来我们来为大家介绍一下 Rust 中的标量类型和复合类型，从标量类型开始：

<p align=center><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/d63e1e3f30944d3fa5188b06512a711f~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2032&h=1364&e=png&b=ddddd6" alt=""  width="50%"/></p>

这些是 Rust 中的整型标量类型，大家只要记得最低从 8 开始，到 128 结束（当然，正常情况下我们最多用到 64，128 在很多平台上需要软件模拟而不是硬件支持，不推荐大家用）；在赋值的时候除了直接十进制数字赋值外，还支持以下语法（大家了解一下就好，不用死记硬背）：

<p align=center><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/1fb30e29d663493b9650defe753653a0~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2224&h=1244&e=png&b=deded8" alt=""  width="50%"/></p>

除了整型之外，Rust 还有以下标量类型：

-   浮点数：f32 / f64
-   bool
-   char：这个比较特殊，Rust 中一个 char 占 4 字节，存放的是一个 UTF-32，而不像 C/C++ 那样本质上是个 u8

除了标量类型之外，Rust 还支持两种复合类型：

-   元组 tuple：let a = (1, 2); let (a, b) = (1, 2)
-   数组 array: let a = [1, 2, 3]; let a = [0; 5] // 这个声明中 0 是默认值，5 是长度，等价于 let a = [0, 0, 0, 0, 0]

# 总结

让我们来总结一下这节的内容，首先我们学习了变量声明的方式：

-   使用 let 定义变量：let a = 1;
-   使用 let mut 定义可变变量：let mut b = 2; b = 3;
-   变量名可复用：let b = 1; let b = b + 1;
-   可以指定变量类型：let a: u64 = 1; let a = 1_u64;

接着，我们学习了 Rust 中的数据类型，如果大家想要了解更多可以阅读一下 The Rust Programming Language（TRPL）的这一章节：https://www.rustwiki.org.cn/zh-CN/book/ch03-02-data-types.html

# 课后练习

最后，给大家留一个课后练习，大家可以尝试一下将标量类型和元组结合起来用，熟悉一下语法～

好了，这就是 Rust 中的变量声明和变量类型的所有内容，是不是非常简单也符合直觉呢？大家都学会了么？我们下节见～

<p align=center><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/089ecf63e53e482f80b0badb25ad489a~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=350&h=167&e=gif&f=20&b=987450" alt=""  width="50%"/></p>