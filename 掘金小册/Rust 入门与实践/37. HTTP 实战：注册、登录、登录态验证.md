# 1. 连接 MySQL
1.  使用 [sqlx](https://github.com/launchbadge/sqlx) 连接数据库，直接执行 SQL。
1.  使用 [Sea-ORM](https://github.com/SeaQL/sea-orm) 进行增删改查。

  


## 准备工作

安装并运行 MySQL，如果没有可以使用 Docker 运行

```Bash
docker run -p 3306:3306 --name mysql -e MYSQL_ROOT_PASSWORD=123456 -d mysql:5.7
```

建表语句：

```SQL
create database test;
use test;
CREATE TABLE `user`
(
    `id`       INTEGER      NOT NULL PRIMARY KEY AUTO_INCREMENT COMMENT 'id',
    `username` VARCHAR(255) NOT NULL COMMENT '用户名',
    `password` VARCHAR(255) NOT NULL COMMENT '密码',
    UNIQUE KEY `uniq_username` (`username`)
) ENGINE = InnoDB
  DEFAULT CHARSET = utf8mb4 COMMENT ='用户';
```

  


如果使用 CLion IDE 可以直接点击右侧的 Database 连接数据库

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/0c204d2160c84dd8b63addb8b5146e8c~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=3838&h=1694&e=png&b=3b3e40)

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/9c1d82ba168a46ea9691b10ec0d578bf~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1600&h=1352&e=png&b=3d4144)

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/5ebae544832047bf915c9ec9a4c186b7~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=3114&h=1090&e=png&b=3b3e40)

  


## 添加依赖

在 Cargo.toml 的 [dependencies] 加入以下内容

```TOML
sqlx = "0.7"
sea-orm = { version = "0.12", features = ["sqlx-mysql", "runtime-tokio-native-tls", "macros", "with-chrono"] }
```

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/4a17734d00f647f184afece972b966c7~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2890&h=1068&e=png&b=2c2c2c)

  


## SQLX 连接数据库

### 创建 src/database/mod.rs

1.  创建 src/database 文件夹
1.  创建 src/database/mod.rs 文件
1.  在上级中声明 database，添加 `pub mod database;` 到 src/lib.rs

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/c09bd436929a42c493d232eabeec0db9~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2358&h=754&e=png&b=2d2d2d)

  


### 创建 src/database/sqlx.rs

需要在上级中（src/database/mod.rs）声明 `pub mod sqlx;`

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/e48cfb17d9e04630bd71297dafeb360a~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2362&h=756&e=png&b=2d2d2d)

  


MySqlPool 是全局变量，**有3种推荐的声明全局变量的方式：**

1.  static 语法，比如 ` static  `*`A`*`: &str = "1";`、 ` static  `*`B`*`: AtomicU16 = AtomicU16::`*`new`*`(1);`，其中 `AtomicU16::new` 是 `pub const fn`
1.  引入 [lazy_static](https://docs.rs/lazy_static/latest/lazy_static/) 依赖，声明需要在运行时执行代码才能初始化的全局变量。包括分配堆内存（Vector、HashMap）和其他需要执行函数调用（非 async）的全局变量，比如 `HashMap::from`。
1.  `tokio::sync::OnceCell`，初始化时需要用到 async 函数调用的代码，比如 async 连接数据库

  


可以运行单元测试中的 test_sqlx 测试是否连接本地数据库成功。

```Rust
use sqlx::MySqlPool;
use tokio::sync::OnceCell;

static POOL: OnceCell<MySqlPool> = OnceCell::const_new();

pub async fn init_pool() -> MySqlPool {
    MySqlPool::connect("mysql://root:123456@localhost:3306/test")
        .await
        .expect("failed to connect database")
}

pub async fn get_pool() -> &'static MySqlPool {
    POOL.get_or_init(init_pool).await
}

#[cfg(test)]
mod tests {
    use super::*;
    use sqlx::Executor;

    #[tokio::test]
    async fn test_sqlx() {
        let pool = get_pool().await;
        let result = pool.fetch_one("select 123").await;
        let _ = dbg!(result);
    }
}
```

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/e11a9cba1aac4d33a00ea3b1286e6989~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=3838&h=2152&e=png&b=2c2c2c)

  


## 使用 Sea-ORM 增删改查

官方文档：[SeaORM 🐚 An async & dynamic ORM for Rust](https://www.sea-ql.org/SeaORM/)

  


### 添加错误类型 sea_orm::DbErr

进行数据库操作时可能有报错，报错类型为 `sea_orm::DbErr`，需要在上一节中的 `HelloError` 添加

  


修改 src/error.rs

```Rust
use axum::http::StatusCode;
use axum::response::{IntoResponse, Response};
use thiserror::Error;

// 为了使用方便，可以用 HelloResult<T> 代替 Result<T, HelloError>
pub type HelloResult<T> = Result<T, HelloError>;

#[derive(Error, Debug)]
pub enum HelloError {
    #[error("not implemented")]
    NotImplemented,
    #[error("not found")]
    NotFound,
    // #[from] 表示自动 impl From<std::io::Error> for HelloError
    // 如果错误类型是 srd::io::Error，通过 ? 返回错误时可以自动转换成 HelloError
    #[error("io error: {0}")]
    IO(#[from] std::io::Error),
    #[error("db error: {0}")]
    Db(#[from] sea_orm::DbErr),
}

impl IntoResponse for HelloError {
    fn into_response(self) -> Response {
        let code = match self {
            Self::NotImplemented => StatusCode::NOT_IMPLEMENTED,
            Self::NotFound => StatusCode::NOT_FOUND,
            Self::IO(_) => StatusCode::INTERNAL_SERVER_ERROR,
            Self::Db(_) => StatusCode::INTERNAL_SERVER_ERROR,
        };
        let body = self.to_string();
        (code, body).into_response()
    }
}
```

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/36adf2b422024f8da00418121d1c5f8d~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2674&h=1878&e=png&b=2c2c2c)

  


### 创建 src/database/sea.rs

> 需要在上级 mod（src/database/mod.rs） 中声明 `pub mod sea;`
>
> 后续章节中不再进行提示。

```Rust
use sea_orm::entity::prelude::*;
use sea_orm::SqlxMySqlConnector;
use tokio::sync::OnceCell;

use super::sqlx::init_pool;

pub async fn init_conn() -> DatabaseConnection {
    let pool = init_pool().await;
    SqlxMySqlConnector::from_sqlx_mysql_pool(pool)
}

static CONN: OnceCell<DatabaseConnection> = OnceCell::const_new();

pub async fn get_conn() -> &'static DatabaseConnection {
    CONN.get_or_init(init_conn).await
}
```

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/1b09124f6c3140cf810db9105fbcf8f1~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2248&h=1030&e=png&b=2d2d2d)

  


### 创建 src/database/user.rs

代码中包含增删改查的基本操作，按以下顺序运行单元测试，查看效果：

1.  test_insert
1.  test_update
1.  test_get
1.  test_delete
1.  test_get

```Rust
use super::sea::get_conn;
use crate::error::HelloResult;
use sea_orm::entity::prelude::*;
use sea_orm::{ActiveValue, DeleteResult, UpdateResult};

#[derive(Clone, Debug, Eq, PartialEq, DeriveEntityModel)]
#[sea_orm(table_name = "user")]
pub struct Model {
    #[sea_orm(primary_key, auto_increment = true)]
    pub id: i32,

    pub username: String,
    pub password: String,
}

#[derive(Copy, Clone, Debug, EnumIter)]
pub enum Relation {}

impl RelationTrait for Relation {
    fn def(&self) -> RelationDef {
        panic!("No RelationDef")
    }
}

impl ActiveModelBehavior for ActiveModel {}

pub async fn insert_user(user: ActiveModel) -> HelloResult<Model> {
    let conn = get_conn().await;
    user.insert(conn).await.map_err(Into::into)
}

pub async fn get_user(username: &str) -> HelloResult<Option<Model>> {
    let conn = get_conn().await;
    Entity::find()
        .filter(Column::Username.eq(username))
        .one(conn)
        .await
        .map_err(Into::into)
}

pub async fn delete_user(username: &str) -> HelloResult<DeleteResult> {
    let conn = get_conn().await;
    Entity::delete_many()
        .filter(Column::Username.eq(username))
        .exec(conn)
        .await
        .map_err(Into::into)
}

pub async fn update_user(username: &str, password: &str) -> HelloResult<UpdateResult> {
    let conn = get_conn().await;
    Entity::update_many()
        .filter(Column::Username.eq(username))
        .set(ActiveModel {
            password: ActiveValue::Set(password.into()),
            // 如果字段过多，可以使用 ..Default::default 表示不需要填写的字段
            ..Default::default()
        })
        .exec(conn)
        .await
        .map_err(Into::into)
}

#[cfg(test)]
mod tests {
    use super::*;
    use sea_orm::ActiveValue;

    #[tokio::test]
    async fn test_insert() {
        let result = insert_user(ActiveModel {
            id: ActiveValue::NotSet, // 有 auto increment，不需要设置
            username: ActiveValue::Set("123".into()),
            password: ActiveValue::Set("123".into()),
        })
        .await;
        println!("{result:?}");
    }

    #[tokio::test]
    async fn test_get() {
        let result = get_user("123").await;
        println!("{result:?}");
    }

    #[tokio::test]
    async fn test_delete() {
        let result = delete_user("123").await;
        println!("{result:?}");
    }

    #[tokio::test]
    async fn test_update() {
        let result = update_user("123", "1234").await;
        println!("{result:?}");
    }
}
```

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/9cc77cfcf154406d80942b49ed088ab4~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2670&h=1908&e=png&b=2c2c2c)

  


## 其他

Clion 可能提示有问题，解决方案：

1.  按 3 次 Shift
1.  搜 Experimental features
1.  打开这些选项

  


![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/a774ecadd7414a1da7b3681cc9a406d9~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1390&h=1366&e=png&b=3e4042)

  


![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/ff87fa2147404c31976f301bc4d32a7f~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1208&h=1266&e=png&b=3f4143)

  


  


  


# 2. 编写注册、登录 handler

## 添加依赖

```TOML
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
```

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/4a4091e845db4550a66d4c418c4dcf08~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2920&h=1256&e=png&b=2c2c2c)

  


## 创建用户 handler

1.  创建 `src/handler` 文件夹和 `src/handler/mod.rs` 文件
1.  声明 handler 模块，添加 `pub mod handler;` 到 `src/lib.rs`
1.  创建 `src/handler/user.rs`
1.  声明 user 模块，添加 `pub mod user;` 到 `src/handler/mod.rs`

  


src/handler/user.rs

handler 参数和返回值的类型必须是 `Json<T>`，因为 `axum::Json` 实现了 `FromRequest` 和 `IntoResponse`（`T` 必须有 `#[derive(Serialize, Deserialize)]`）

在前面的章节中介绍过，handler 函数的参数必须实现 `FromRequest`，返回值必须实现 `IntoResponse`，如果暂时不能理解也没关系，照着抄就行（**别抄漏了 Json**）

```Rust
use crate::database::user;
use crate::database::user::{get_user, insert_user};
use crate::error::HelloResult;
use axum::Json;
use sea_orm::ActiveValue;
use serde::{Deserialize, Serialize};

#[derive(Serialize, Deserialize, Clone, Debug, Default)]
pub struct RegisterRequest {
    pub username: String,
    pub password: String,
}

#[derive(Serialize, Deserialize, Clone, Debug, Default)]
pub struct RegisterResponse {
    pub id: i32,
}

pub async fn register(Json(req): Json<RegisterRequest>) -> HelloResult<Json<RegisterResponse>> {
    let user = insert_user(user::ActiveModel {
        username: ActiveValue::Set(req.username),
        password: ActiveValue::Set(req.password),
        ..Default::default()
    })
    .await?;
    Ok(Json(RegisterResponse { id: user.id }))
}

#[derive(Serialize, Deserialize, Clone, Debug, Default)]
pub struct LoginRequest {
    pub username: String,
    pub password: String,
}

#[derive(Serialize, Deserialize, Clone, Debug, Default)]
pub struct LoginResponse {
    pub success: bool,
    pub token: String,
}

pub async fn login(Json(req): Json<LoginRequest>) -> HelloResult<Json<LoginResponse>> {
    let user = get_user(&req.username).await?;
    let user = match user {
        Some(user) => user,
        None => {
            return Ok(Json(LoginResponse {
                success: false,
                ..Default::default()
            }))
        }
    };
    if req.password != user.password {
        return Ok(Json(LoginResponse {
            success: false,
            ..Default::default()
        }));
    }
    // 这里偷个懒，token 就是 username + password 的 json
    Ok(Json(LoginResponse {
        success: true,
        token: serde_json::to_string(&req).unwrap(),
    }))
}
```

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/2df61575c4ec47239685b43685cadeef~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2674&h=2026&e=png&b=2c2c2c)

  


## 添加 handler 到 router

src/bin/server.rs

```Rust
use axum::routing::{get, post};
use axum::{Router, Server};
use hello_axum::handler::user::{login, register};
use hello_axum::{hello, not_found_handler, not_implemented_handler, read_file_handler};
use std::net::SocketAddr;

#[tokio::main]
async fn main() {
    tracing_subscriber::fmt::init();

    let app = Router::new()
        .route("/hello", get(hello))
        .nest(
            "/error",
            Router::new()
                .route("/not_implemented", get(not_implemented_handler))
                .route("/read_file", get(read_file_handler)),
        )
        .nest(
            "/user",
            Router::new()
                .route("/register", post(register))
                .route("/login", post(login)),
        )
        .fallback(not_found_handler);

    // run it
    let addr = SocketAddr::from(([0, 0, 0, 0], 3000));
    tracing::info!("listening on {addr}");
    Server::bind(&addr)
        .serve(app.into_make_service())
        .await
        .unwrap();
}
```

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/216f677c4b3b477a81cc1ae60771585b~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2982&h=1942&e=png&b=2c2c2c)

  


## 测试

```Bash
curl --location 'http://localhost:3000/user/register' \
--header 'Content-Type: application/json' \
--data '{
    "username": "hello",
    "password": "123"
}'


curl --location 'http://localhost:3000/user/login' \
--header 'Content-Type: application/json' \
--data '{
    "username": "hello",
    "password": "123"
}'
```

  


# 3. 登录态验证

<!---->

1.  从 cookie 中提取 token，如果 token 不存在，未登录

1.  解析 token 变为自定义结构体 User

1.  定义3种 handler 测试是否成功

    1.  必须登录的 handler
    1.  可选登录的 handler
    1.  不需要登录的 handler

为了简化逻辑（偷懒），token 的形式为 json（username + password），不采用 jsonwebtoken

  


## 添加依赖

> 因为 Rust 的 stable 版还不支持 async-trait 所以需要添加依赖库。

```Rust
async-trait = "0.1"
```

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/db13271545bb43dbb2708522fb0400e8~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2922&h=1342&e=png&b=2c2c2c)

  


## 添加错误类型

修改 src/error.rs

```Rust
use axum::http::StatusCode;
use axum::response::{IntoResponse, Response};
use thiserror::Error;

// 为了使用方便，可以用 HelloResult<T> 代替 Result<T, HelloError>
pub type HelloResult<T> = Result<T, HelloError>;

#[derive(Error, Debug)]
pub enum HelloError {
    #[error("not implemented")]
    NotImplemented,
    #[error("not found")]
    NotFound,
    // #[from] 表示自动 impl From<std::io::Error> for HelloError
    // 如果错误类型是 srd::io::Error，通过 ? 返回错误时可以自动转换成 HelloError
    #[error("io error: {0}")]
    IO(#[from] std::io::Error),
    #[error("db error: {0}")]
    Db(#[from] sea_orm::DbErr),
    #[error("forbidden")]
    Forbidden,
}

impl IntoResponse for HelloError {
    fn into_response(self) -> Response {
        let code = match self {
            Self::NotImplemented => StatusCode::NOT_IMPLEMENTED,
            Self::NotFound => StatusCode::NOT_FOUND,
            Self::IO(_) => StatusCode::INTERNAL_SERVER_ERROR,
            Self::Db(_) => StatusCode::INTERNAL_SERVER_ERROR,
            Self::Forbidden => StatusCode::FORBIDDEN,
        };
        let body = self.to_string();
        (code, body).into_response()
    }
}
```

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/f2e91685e9f54705bd3c342a5a9d20ce~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2684&h=2026&e=png&b=2c2c2c)

  


## 创建 src/handler/auth.rs

1.  定义 User 结构体，为 User 实现 `FromRequestParts`，可能存在 ` Ok(User)  `和 `Err(Rejection)` 两种情况，其中 Rejection 的类型为 `HelloError`
1.  如果从 Parts 中获取 cookie 失败，返回 `HelloError::Forbidden`
1.  如果从 cookie 中获取 token 失败，返回 `HelloError::Forbidden`
1.  如果解析 token 失败，返回 `HelloError::Forbidden`

```Rust
use crate::error::HelloError;
use axum::extract::FromRequestParts;
use axum::headers::Cookie;
use axum::http::request::Parts;
use axum::{RequestPartsExt, TypedHeader};
use serde::{Deserialize, Serialize};

#[derive(Serialize, Deserialize, Clone, Debug, Default)]
pub struct User {
    pub username: String,
}

#[async_trait::async_trait]
impl<S> FromRequestParts<S> for User {
    type Rejection = HelloError;

    async fn from_request_parts(parts: &mut Parts, _: &S) -> Result<Self, Self::Rejection> {
        let cookie = parts
            .extract::<TypedHeader<Cookie>>()
            .await
            .map_err(|_| HelloError::Forbidden)?;
        let token = cookie.get("token").ok_or(HelloError::Forbidden)?;
        let user = serde_json::from_str(token).map_err(|_| HelloError::Forbidden)?;
        Ok(user)
    }
}
```

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/20fca3b9de0b46c9a1e1808a9a27ad8f~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2672&h=1520&e=png&b=2c2c2c)

  


## 创建 src/handler/check.rs

-   因为 User 实现了 `FromRequestParts`，所以可以作为 handler 的参数（可以参考前面的章节）
-   如果参数中带有 `User`，必须解析 token 成功，否则不执行 handler，返回上方定义的 `Rejection`
-   如果参数中带有 `Option<User>`，无论 token 是否解析成功，都会执行 handler，如果不成功为 None
-   如果参数中不带有 User，不执行 User 解析

```Rust
use crate::handler::auth::User;

pub async fn must_login(
    user: User,
    // 除了 User，可以添加多个其他 FromRequest/FromRequestParts 参数
    // 最多有一个实现了 FromRequest 的参数（消耗 body 所有权），比如 Json<T>
    // 可以有多个实现了 FromRequestParts 的参数（不消耗 body 所有权），比如 Query<T>、HeaderMap
) -> String {
    format!("must_login, username: {}", user.username)
}

pub async fn optional_login(user: Option<User>) -> String {
    match user {
        None => String::from("optional_login, not login"),
        Some(user) => format!("optional_login, username: {}", user.username),
    }
}

pub async fn no_login() -> String {
    String::from("no login")
}
```

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/e4b28cc2bb8b43029d4c2e214b1b8463~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2644&h=1272&e=png&b=2c2c2c)

  


## 添加 check handler 到 router

```Rust
use axum::routing::{get, post};
use axum::{Router, Server};
use hello_axum::handler::check::{must_login, no_login, optional_login};
use hello_axum::handler::user::{login, register};
use hello_axum::{hello, not_found_handler, not_implemented_handler, read_file_handler};
use std::net::SocketAddr;

#[tokio::main]
async fn main() {
    tracing_subscriber::fmt::init();

    let app = Router::new()
        .route("/hello", get(hello))
        .nest(
            "/error",
            Router::new()
                .route("/not_implemented", get(not_implemented_handler))
                .route("/read_file", get(read_file_handler)),
        )
        .nest(
            "/user",
            Router::new()
                .route("/register", post(register))
                .route("/login", post(login)),
        )
        .nest(
            "/check",
            Router::new()
                .route("/must_login", get(must_login))
                .route("/optional_login", get(optional_login))
                .route("/no_login", get(no_login)),
        )
        .fallback(not_found_handler);

    // run it
    let addr = SocketAddr::from(([0, 0, 0, 0], 3000));
    tracing::info!("listening on {addr}");
    Server::bind(&addr)
        .serve(app.into_make_service())
        .await
        .unwrap();
}
```

  


## 测试

```Bash
curl -i 'http://localhost:3000/check/must_login'
curl -i 'http://localhost:3000/check/must_login' --header 'Cookie: token={"username":"hello","password":"123"}'

curl -i 'http://localhost:3000/check/optional_login'
curl -i 'http://localhost:3000/check/optional_login' --header 'Cookie: token={"username":"hello","password":"123"}'

curl -i 'http://localhost:3000/check/no_login'
curl -i 'http://localhost:3000/check/no_login' --header 'Cookie: token={"username":"hello","password":"123"}'
```

  


 # 4. 习题



1.  给 user 添加一些字段（permission、role、updated_at、created_at）
1.  把 login 返回的 token 改为 jsonwebtoken
1.  解析 jwt token

  


参考资料：

1.  [axum-jwt-example](https://github.com/tokio-rs/axum/tree/main/examples/jwt)
1.  https://jwt.io/

  


# 总结

知识点：

-   三种声明全局变量的方式，数据库连接池保存在 `OnceCell` 中
-   使用 sqlx 连接数据库，直接执行 SQL
-   使用 Sea-ORM 进行增删改查
-   使用 `..Default::default` 填充结构体内的默认字段
-   在 handler 参数/返回值中使用 `axum::Json<T>`
-   定义 `User` 结构体，并实现 `FromRequestParts` 从请求头 cookie 中解析用户信息。在 handler 参数中使用 `User` 和 `Option<User>`