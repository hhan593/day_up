
在本文开始前，请确保有必要的素材文件、基础的项目搭建，接下来我们将逐步实现这个游戏：

<p align=center><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/71ab0e43b5c547b988ed11cc12d1fb55~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=594&h=585&s=136817&e=png&b=2f6f28" alt="image.png"  /></p>

如果你对 API 不熟悉，强烈建议在需要用的时候，查阅文档：
[Physics - Phaser 3 API Documentation (beta)](https://newdocs.phaser.io/docs/3.60.0/scenes) 

那么接下来，我将从游戏机制、物理对象、状态控制、游戏逻辑、优化等方面来讲述。



## 游戏基础机制

在最开始的环节，我将介绍游戏初始化阶段，主要包括主入口配置、如何场景切换、生命周期以及事件机制在游戏中的应用、游戏初始化类 Game。

### 游戏主入口

```js
import Phaser from "phaser";

import Game from "./scenes/Game";
import UI from "./scenes/UI";
import GameOver from "./scenes/GameOver";
import AudioPlay from "./scenes/AudioPlay";
import GameStart from "./scenes/GameStart";
import GameSuc from "./scenes/GameSuc";

const config: Phaser.Types.Core.GameConfig = {
  type: Phaser.AUTO,
  width: 600,
  height: 600,
  physics: {
    default: "matter",
    matter: {
      debug: false, 
    },
  },
  scene: [GameStart, Game, UI, AudioPlay, GameOver, GameSuc],
};

export default new Phaser.Game(config);
```

使用 Phaser 框架创建一个游戏的配置。它导入了一些场景文件，包括 Game、UI、GameOver、AudioPlay、GameStart 和 GameSuc。通过 scene 我们可以快速切换场景，注意前后的渲染顺序。

在配置物理引擎中，我们使用了最常用的 Matter.js 物理引擎，因为 Matter.js 可以做强大的物理效果同时又灵活，除了 Matter.js 物理引擎，Phaser 框架还支持 Arcade Physics 和 Impact Physics。

Arcade Physics 是 Phaser 的默认物理引擎，是一个轻量级的、基于速度的引擎。它适用于简单的碰撞检测和基本的物理效果。Arcade Physics 易于使用和配置，适合 2D 平台游戏和街机风格的游戏。然而，它的功能相对较简单，不支持复杂的物理效果。

Impact Physics 是一个可选的物理引擎，它提供了更高级的功能和更复杂的物理效果。它支持更精确的碰撞检测和响应，以及更多的物理特性，例如摩擦力、弹性碰撞和关节。Impact Physics 适用于需要更复杂物理效果的游戏。

除了 `Phaser.AUTO` 会自动使用最佳的渲染器类型，`type` 属性还可以取以下值：
- `Phaser.CANVAS` ：使用 Canvas 渲染器进行游戏渲染。 
- `Phaser.WEBGL` ：使用 WebGL 渲染器进行游戏渲染。 
- `Phaser.HEADLESS` ：无渲染模式，用于在服务器上运行游戏逻辑而无需实际渲染画面。

我们通过设置 debug 为 true，可以查看物体的线框，代表是否是真实物体，这点在开发中一定要开启以方便调试。

<p align=center><img src="https://p6-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/384e2b1d76e14304b3385df2ca2356b6~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=362&h=373&s=60905&e=png&b=307129" alt="image.png"  /></p>

最后，通过创建一个 Phaser.Game 实例来启动游戏。



### 游戏场景切换

```js
import Phaser from "phaser";
import { sharedInstance as events } from "./EventCenter";

export default class GameStart extends Phaser.Scene {
  constructor() {
    super("game-start");
  }
  preload() {
    this.load.image("start", "assets/start.png");
    this.load.image("bg", "assets/bg.png");
  }
  create() {
    const { width, height } = this.scale;
    this.add.image(width * 0.5, height * 0.3, "bg").setScale(1.2);
    this.add
      .text(width * 0.5, height * 0.3, "Forest Gate", {
        fontSize: "100px",
        color: "#fff",
        fontFamily: "Arial",
        stroke: "#00ff00", // 绿色外边框颜色
        strokeThickness: 10, // 外边框粗细
        shadow: {
          offsetX: 2, // 阴影横向偏移
          offsetY: 2, // 阴影纵向偏移
          color: "#000", // 阴影颜色
          blur: 4, // 阴影模糊程度
          stroke: true, // 是否为阴影添加外边框
          fill: true, // 是否填充阴影
        },
      })
      .setOrigin(0.5);
    
    // 按钮点击开始游戏
    const button = this.add
      .rectangle(width * 0.5, height * 0.55, 150, 75, 0xffffff)
      .setInteractive()
      .on(Phaser.Input.Events.GAMEOBJECT_POINTER_UP, () => {
        events.emit("play_menuClick");
        this.scene.start("game");
      });
    this.add.image(button.x, button.y + 10, "start").setScale(0.5);
  }
}
```

通过继承 Phaser.Scene，我们可以通过 ` this.scene.start("game");` 来切换不同的场景，传入的参数为 key，就是 `super("game-start");` 用于告诉父类当前注册的 key 是什么。在 Phaser 中 key 是很重要的东西，任何资源的加载、对象等，我们都需要写入 Key，一旦其他地方需要使用的时候，就可以直接通过 key 去查询。

我们可以通过 load 预加载资源、在 create 中执行图形处理，通过 `add.image` 来加载图片资源，`add.text` 来加载文本，最后我们在图片的上面覆盖一层矩形热区范围，一旦点击触发对应的事件和切换游戏场景。

<p align=center><img src="https://p1-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/d9e93b81c670410799a074f14737bc32~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=577&h=510&s=151113&e=png&b=3a772d" alt="image.png"  /></p>

包括其他的场景构建，比如游戏失败、成功，本文将不再赘述，接下来我们讲下生命周期。


### Phaser 的生命周期

通过继承 Phaser.Scene，在 scene 队列中我们注册了场景对象，然后它会按顺序执行。每一个场景对象都存在一个生命周期过程：`init()` -> `preload()` -> `create()`-> `update()` -> `destroy()`。

在每个场景中，`init()` 方法在场景启动前调用，我们会用于做一些事件监听和一些类和变量的初始化，`preload()` 方法用于预加载资源，`create()` 方法用于创建场景元素。而 `update()` 方法会在切换后的新场景中开始执行，一般用于角色的行为更新。这些方法的执行顺序是固定的，并且每个场景都会按照这个顺序依次执行。 

需要注意的是，当从一个场景切换到另一个场景时，前一个场景的 `shutdown()` 方法会在新场景的 `init()` 方法执行之前调用。


### Phaser 的事件机制

在之前我们监听点击按钮触发开始游戏事件，其实就是个发布订阅模式，在 Phaser 中已经为我们做了封装：

```js
const emitter = new Phaser.Events.EventEmitter(); 
// 注册事件监听器
emitter.on('custom_event', (data) => { console.log('Event triggered:', data); }); 
// 触发事件 
emitter.emit('custom_event', { message: 'Hello, Phaser!' });
```


### Game 游戏类

在这个类中我们会执行大部门游戏的逻辑，比如启动游戏资源、音频、设置 Matterjs 碰撞、加载 Tiled 瓦片图等。


```js
//...
export default class Game extends Phaser.Scene {
  private cursors!: Phaser.Types.Input.Keyboard.CursorKeys;

  private panda?: Phaser.Physics.Matter.Sprite;
  private playerController?: PlayerController;
  private obstacles!: ObstaclesController;
  private snake: SnakeController[] = [];

  constructor() {
    super("game");
  }

  init() {
    this.cursors = this.input.keyboard.createCursorKeys();
    // 用于存取id，判断哪个物体的障碍类
    this.obstacles = new ObstaclesController();
    this.snake = [];
    
    // 一旦场景触发 SHUTDOWN，销毁游戏
    this.events.once(Phaser.Scenes.Events.SHUTDOWN, () => {
      this.destroy();
    });
    
    events.on("game-suc", () => {
      this.scene.start("game-suc");
    });
  }

  preload() {
    // 加载精灵表
    this.load.atlas("panda", "assets/panda.png", "assets/panda.json");
    this.load.atlas("snake", "assets/snake.png", "assets/snake.json");
    
    // 加载瓦片图
    this.load.image("tiles", "assets/tiles_spritesheet.png");
    this.load.image("tiles", "assets/sheet.png");
    this.load.tilemapTiledJSON("tilemap", "assets/game.json");
    
    // 加载外部资源图片
    this.load.image("star", "assets/star.png");
    this.load.image("health", "assets/health.png");
    this.load.image("key", "assets/key.png");
  }

  create() {
    // 启动ui 画面场景和 音频场景对象
    this.scene.launch("ui");
    this.scene.launch("audio");
    
    // 注意这里设置的原点在左上角，在phaser中是中心坐标
    this.add.image(0, 0, "bg").setScale(1.3).setOrigin(0, 0);
    this.add.image(1600, 0, "bg").setScale(1.3).setOrigin(0, 0);
    this.add.image(3200, 0, "bg").setScale(1.3).setOrigin(0, 0);
    
    // 加载瓦片图
    const map = this.make.tilemap({ key: "tilemap" });
    const tileset = map.addTilesetImage(
      "tiles_spritesheet",
      "tiles"
    ) as Phaser.Tilemaps.Tileset;
    
    // 设置碰撞属性
    const ground = map.createLayer(
      "ground",
      tileset
    ) as Phaser.Tilemaps.TilemapLayer;

    ground.setCollisionByProperty({ collides: true });
    
    // 生成图层
    map.createLayer("obstacles", tileset);
    map.createLayer("door", tileset);
    const hideLayer = map.createLayer(
      "hide",
      tileset
    ) as Phaser.Tilemaps.TilemapLayer;
    hideLayer.setVisible(false);
    
    // 加载对象层
    const objectsLayer = map.getObjectLayer(
      "objects"
    ) as Phaser.Tilemaps.ObjectLayer;

    objectsLayer.objects.forEach((objData) => {
      const { x = 0, y = 0, name, width = 0, height = 0 } = objData;
      switch (name) {
        case "panda-spawn": {
          this.panda = this.matter.add
            .sprite(x + width * 0.5, y, "panda")
            .setFixedRotation();

          this.playerController = new PlayerController(
            this,
            this.panda,
            this.cursors,
            this.obstacles
          );

          this.cameras.main.startFollow(this.panda, true);
          break;
        }

        case "snake": {
          const snake = this.matter.add
            .sprite(x, y, "snake")
            .setFixedRotation();

          this.snake.push(new SnakeController(this, snake));
          // 存储障碍物
          this.obstacles.add("snake", snake.body as MatterJS.BodyType);
          break;
        }
        case "star": {
          //...
        }
        case "key": {
          //...
        }
        case "health": {
          //...
        }
        case "spikes": {
          //...
        }
        case "hide": {
          //...
        }
        case "door": {
          //...
        }
      }
    });

    this.matter.world.convertTilemapLayer(ground);
  }
  destroy() {
    //...
  }
  update(t: number, dt: number) {
    //...
  }
}
```

上文代码已经做了核心的注释，在加载对象层中，我们对 `objectsLayer.objects` 返回的不同 name 做不同的逻辑处理，同时要绑定生成物理对象。





## 创建物理对象

在这个阶段，我们将从 Tiled 导出的瓦片属性与 Matterjs 物理引擎做相应的绑定，生成不同物体的物理网格对象，这样方便我们后期的碰撞检测。


### ObstaclesController 碰撞检测类

```js
const createKey = (name: string, id: number) => {
  return `${name}-${id}`;
};

export default class ObstaclesController {
  private obstacles = new Map<string, MatterJS.BodyType>();

  add(name: string, body: MatterJS.BodyType) {
    const key = createKey(name, body.id);
    if (this.obstacles.has(key)) {
      throw new Error("obstacle already exists at this key");
    }
    this.obstacles.set(key, body);
  }

  is(name: string, body: MatterJS.BodyType) {
    const key = createKey(name, body.id);
    if (!this.obstacles.has(key)) {
      return false;
    }
    return true;
  }
}

```
在之前的 Game 类中，我们创建了 ObstaclesController，以及不断添加障碍物（一种类型有多个存在的）到 obstacles 中，通过封装 ObstaclesController 在碰撞时判断是哪一个类型的实例对象。

通过 add 方法添加到 Map 中存储，后续用于碰撞检测时我们直接用 is 方法即可。


### 绑定到 Matterjs 生成真实物理对象

为了让物体能真实存在于模拟的物理世界，我们需要在加载后创建不同的物理对象，这个时候 Tiled 导出的 json 文件中的 name 名称就很重要了，我们需要通过这个 name 来匹配。


```js
// 加载对象层 
const objectsLayer = map.getObjectLayer( "objects" ) as Phaser.Tilemaps.ObjectLayer; 
objectsLayer.objects.forEach((objData) => {
    const { x = 0, y = 0, name, width = 0, height = 0 } = objData;
      switch (name) { 
        //... 
      }
}
```

### 绑定小熊猫

```js
case "panda-spawn": {
  this.panda = this.matter.add
    .sprite(x + width * 0.5, y, "panda")
    .setFixedRotation();

  this.playerController = new PlayerController(
    this,
    this.panda,
    this.cursors,
    this.obstacles
  );

  this.cameras.main.startFollow(this.panda, true);
  break;
}
```

- 使用 `this.matter.add.sprite()` 方法创建了一个精灵对象，位置位于 `x + width * 0.5` 和 `y` 处，使用了名为 "panda" 的纹理。
- 然后调用了 `.setFixedRotation()` 方法，用于设置精灵的旋转固定，即禁止精灵在物理世界中发生旋转。 
- 创建一个`playerController`实例，用于控制熊猫的操作逻辑。
- 最后使用 `this.cameras.main.startFollow()` 方法，将主摄像机跟随 panda 精灵，第二个参数 `true` 表示摄像机会以平滑的方式跟随精灵。


### 绑定毒蛇

```js
case "snake": {
  const snake = this.matter.add
    .sprite(x, y, "snake")
    .setFixedRotation();

  this.snake.push(new SnakeController(this, snake));
  this.obstacles.add("snake", snake.body as MatterJS.BodyType);
  break;
}
```

这里同样我们生成了蛇的精灵对象，然后将所有 snake 收集了起来，并添加到 obstacles 中，方便我们在其他控制类中判断是哪个精灵实例碰撞的。我们一般用于敌人、尖刺。

而消耗品，比如爱心、钥匙等就没有必要了，我们直接 `setData` 就存储状态。就比如下面的爱心瓦片。

### 绑定爱心

```js
case "health": {
  const health = this.matter.add.sprite(x, y, "health", undefined, {
    isStatic: true,
    isSensor: true,
  });

  health.setData("type", "health");
  health.setData("healthPoints", 10);
  break;
}
```

我们设置对应的 type 为 health，一旦人物触碰到后，我们可以根据 type 去匹配对应的物体，然后处理对应吃到爱心时，我们可以处理相关的逻辑。

在 Game 类的对象层中我们主要生成物理对象，绑定物理对象的状态。



## 状态控制

在这个环节，我们将首先创建一个 Store 用于管理全局状态，然后对玩家控制的对象做不同精灵动画的状态控制、碰撞检测等。


### 全局状态 Store

`Store` 的类，只是实现了一个简单的键值存储功能，通过单例模式只存在一个实例，然后用于在不同类中的共享状态。


```js
class Store {
  data: any = {};
  static instance;

  constructor() {
    this.data = new Map();
  }
  static getInstance() {
    if (!Store.instance) {
      Store.instance = new Store();
    }
    return Store.instance;
  }

  get(key) {
    return this.data.get(key);
  }

  set(key, value) {
    this.data.set(key, value);
  }

  delete(key) {
    this.data.delete(key);
  }
}

export default Store.getInstance();
```




### 玩家控制类

PlayerController 类，我们将主要处理熊猫的交互逻辑，比如跳、移动、闲置、死亡的动画处理，包括触碰到敌人、触碰到爱心、触碰到钥匙等。

#### 加载精灵图

第一步，我们需要加载精灵图：

```js
export default class PlayerController {
  private scene: Phaser.Scene;
  private sprite: Phaser.Physics.Matter.Sprite;
  // ... 
  constructor(
    scene: Phaser.Scene,
    sprite: Phaser.Physics.Matter.Sprite,
    cursors: CursorKeys,
    obstacles: ObstaclesController
  ) {
    // ...
    this.createAnimations();
```

在实例化 PlayerController 的时候，我们要创建对应的动画：

```js
 private createAnimations() {
    this.sprite.anims.create({
      key: "player-idle",
      frames: [{ key: "panda", frame: "panda_idle_01.png" }],
    });

    this.sprite.anims.create({
      key: "player-walk",
      frameRate: 10,
      frames: this.sprite.anims.generateFrameNames("panda", {
        start: 1,
        end: 4,
        prefix: "panda_01_run_0",
        suffix: ".png",
      }),
      repeat: -1,
    });

    this.sprite.anims.create({
      key: "player-death",
      frameRate: 10,
      frames: this.sprite.anims.generateFrameNames("panda", {
        start: 1,
        end: 5,
        prefix: "panda_01_die_0",
        suffix: ".png",
      }),
      repeat: 0,
    });
  }
```

使用 `this.sprite.anims.create()` 方法创建了三个动画，分别是 "player-idle"、"player-walk" 和 "player-death"。每个动画都使用了一系列帧，并指定了帧率和重复次数等属性。这样就创建了一组动画，用于在游戏中播放和控制精灵的动作。

现在剩下的就是让精灵根据键盘输入事件播放对应的精灵图动画，我们知道不同的行为对应的就是几帧图片的切换，意味着有开始和结束，所以最好能监听到开始事件、更新事件、结束事件。

比如奔跑行为，第一次我们可以通过 `this.sprite.play("player-walk");` 执行初始化对应 `onEnter`，另外在 update 生命周期中一旦状态变化就会更新，比如我们可以通过 `this.cursors = this.input.keyboard.createCursorKeys();`监听键盘事件就会触发 update，在 update 中我们执行精灵图的播放。那么就触发对应 `onUpdate`， 一旦切换其他的行为，意味着上个行为要执行结束，所以上个行为就会触发 `onExit`。

那么，我们通过 name 为 key，对应三种不同状态的回调，可以手写一个状态 store 来模拟行为的状态。

#### StateMachine 类

```js
interface IState {
  name: string;
  onEnter?: () => void;
  onUpdate?: (dt: number) => void;
  onExit?: () => void;
}

let idCount = 0;

export default class StateMachine {
  private id = (++idCount).toString();
  private context?: object;
  private states = new Map<string, IState>();

  private previousState?: IState;
  private currentState?: IState;
  private isChangingState = false;
  private changeStateQueue: string[] = [];

  get previousStateName() {
    if (!this.previousState) {
      return "";
    }

    return this.previousState.name;
  }

  constructor(context?: object, id?: string) {
    this.id = id ?? this.id;
    this.context = context;
  }

  isCurrentState(name: string) {
    if (!this.currentState) {
      return false;
    }

    return this.currentState.name === name;
  }

  addState(
    name: string,
    config?: {
      onEnter?: () => void;
      onUpdate?: (dt: number) => void;
      onExit?: () => void;
    }
  ) {
    const context = this.context;

    this.states.set(name, {
      name,
      onEnter: config?.onEnter?.bind(context),
      onUpdate: config?.onUpdate?.bind(context),
      onExit: config?.onExit?.bind(context),
    });

    return this;
  }

  setState(name: string) {
    if (!this.states.has(name)) {
      console.warn(`Tried to change to unknown state: ${name}`);
      return;
    }

    if (this.isCurrentState(name)) {
      return;
    }

    if (this.isChangingState) {
      this.changeStateQueue.push(name);
      return;
    }

    this.isChangingState = true;

    console.log(
      `[StateMachine (${this.id})] change from ${
        this.currentState?.name ?? "none"
      } to ${name}`
    );

    if (this.currentState && this.currentState.onExit) {
      this.currentState.onExit();
    }

    this.previousState = this.currentState;
    this.currentState = this.states.get(name)!;

    if (this.currentState.onEnter) {
      this.currentState.onEnter();
    }

    this.isChangingState = false;
  }

  update(dt: number) {
    if (this.changeStateQueue.length > 0) {
      this.setState(this.changeStateQueue.shift()!);
      return;
    }

    if (this.currentState && this.currentState.onUpdate) {
      this.currentState.onUpdate(dt);
    }
  }
}
```

通过使用这个状态机类，你可以定义不同的状态，添加状态回调函数，并在适当的时候切换状态。状态机会自动调用相应的回调函数，以实现状态之间的切换和逻辑的处理。


#### 注册状态

```js
this.stateMachine = new StateMachine(this, "player");

this.stateMachine
  .addState("idle", {
    onEnter: this.idleOnEnter,
    onUpdate: this.idleOnUpdate,
  })
  .addState("walk", {
    onEnter: this.walkOnEnter,
    onUpdate: this.walkOnUpdate,
    onExit: this.walkOnExit,
  })
  .addState("jump", {
    onEnter: this.jumpOnEnter,
    onUpdate: this.jumpOnUpdate,
    onExit: this.jumpOnExit,
  })
  .addState("spike-hit", {
    onEnter: this.spikeHitOnEnter,
  })
  .addState("snake-hit", {
    onEnter: this.snakeHitOnEnter,
  })
  .addState("snake-stomp", {
    onEnter: this.snakeStompOnEnter,
  })
  .addState("dead", {
    onEnter: this.deadOnEnter,
  })
  .setState("idle");
```

首先，创建了一个名为 `stateMachine` 的 `StateMachine` 实例，并将当前对象（`this`）和字符串 "player" 作为参数传递进去。这个字符串是状态机的唯一 ID。

使用 `stateMachine.addState()` 方法添加了多个状态，并为每个状态指定了进入、更新和退出的回调函数。具体的状态和回调函数如下： 

- idle **闲置状态**：进入时调用 `idleOnEnter` ，更新时调用 `idleOnUpdate`。 
- walk **跑步状态**：进入时调用 `walkOnEnter` ，更新时调用 `walkOnUpdate` ，退出时调用 `walkOnExit`。 
- jump **跳跃状态**：进入时调用 `jumpOnEnter` ，更新时调用 `jumpOnUpdate` ，退出时调用 `jumpOnExit`。 
- spike-hit **被尖刺触碰状态**：进入时调用 `spikeHitOnEnter`。 
- snake-hit **被蛇触碰状态**：进入时调用 `snakeHitOnEnter`。 
- snake-stomp **踩扁蛇状态**：进入时调用 `snakeStompOnEnter`。
- dead **死亡状态**：进入时调用 `deadOnEnter`。


#### 检测碰撞

```js
 this.sprite.setOnCollide((data: MatterJS.ICollisionPair) => {
      const body = data.bodyB as MatterJS.BodyType;
        
       // 检测是否是尖刺
      if (this.obstacles.is("spikes", body)) {
        this.stateMachine.setState("spike-hit");
        events.emit("play_hitWall");

        return;
      }
    
      // 检测是否是门
      if (this.obstacles.is("door", body)) {
        // 播放音乐
        events.emit("play_eat");
        // 切换成功场景对象
        events.emit("game-suc");
        return;
      }
        
      // 检测是否是蛇
      if (this.obstacles.is("snake", body)) {
        this.lastSnake = body.gameObject;
        if (this.sprite.y + 30 < body.position.y) {
          // 设置行为状态, 击杀毒蛇
          this.stateMachine.setState("snake-stomp");
          events.emit("play_eat");
        } else {
          // 设置行为状态, 受到毒蛇伤害
          this.stateMachine.setState("snake-hit");
          events.emit("play_hitWall");
        }
        return;
      }

      const gameObject = body.gameObject;

      if (!gameObject) return;
      
      // 当遇到瓦片体使其状态改为静止
      if (gameObject instanceof Phaser.Physics.Matter.TileBody) {
        if (this.stateMachine.isCurrentState("jump")) {
          this.stateMachine.setState("idle");
        }
        return;
      }

      const sprite = gameObject as Phaser.Physics.Matter.Sprite;
      const type = sprite.getData("type");
        
      // 根据之前 Matterjs设置的属性值匹配对应碰撞的类型
      switch (type) {
        case "star": {
          events.emit("star-collected");
          events.emit("play_eat");

          sprite.destroy();
          break;
        }

        case "health": {
          const value = sprite.getData("healthPoints") ?? 10;
          this.health = Phaser.Math.Clamp(this.health + value, 0, 100);
          events.emit("health-changed", this.health);
          events.emit("play_eat");

          sprite.destroy();
          break;
        }

        case "key": {
          events.emit("play_eat");
          const hideLayer = sprite.getData("hideLayer");

          const hide = store.get("hide");
          hideLayer.setVisible(true);
          hide.isSensor = false;
          sprite.destroy();
          break;
        }
      }
    });
```

#### 不同状态的逻辑

```js
this.stateMachine.addState("snake-hit", {
    onEnter: this.snakeHitOnEnter,
  })
  .addState("snake-stomp", {
    onEnter: this.snakeStompOnEnter,
  })
      
  //...
  if (this.obstacles.is("snake", body)) {
    this.lastSnake = body.gameObject;
    if (this.sprite.y + 30 < body.position.y) {
      this.stateMachine.setState("snake-stomp");
      events.emit("play_eat");
    } else {
      this.stateMachine.setState("snake-hit");
      events.emit("play_hitWall");
    }
    return;
  }
```

如果角色与毒蛇障碍物发生碰撞，会执行以下逻辑：

将碰撞到的 "snake" 障碍物对象存储到 `this.lastSnake` 变量中。如果角色的 y 坐标加上 30 小于障碍物的 y 坐标，表示角色从上方踩踏了 "snake" 障碍物，此时将状态切换为 "snake-stomp"，并通过事件 `events.emit("play_eat")` 触发播放吃东西的声音。 否则，表示角色撞到了 "snake" 障碍物，此时将状态切换为 "snake-hit"，并通过事件 `events.emit("play_hitWall")` 触发播放撞墙的声音。




## 游戏逻辑处理

在这个环节，我们将了解到游戏游玩中的一些交互逻辑，比如玩家受到伤害、消灭敌人、控制敌人定时巡逻等。

### 玩家受到伤害

```js
private snakeHitOnEnter() {
    if (this.lastSnake) {
      if (this.sprite.x < this.lastSnake.x) {
        this.sprite.setVelocityX(-20);
      } else {
        this.sprite.setVelocityX(20);
      }
    } else {
      this.sprite.setVelocityY(-20);
    }

    const startColor = Phaser.Display.Color.ValueToColor(0xffffff);
    const endColor = Phaser.Display.Color.ValueToColor(0x0000ff);

    this.scene.tweens.addCounter({
      from: 0,
      to: 100,
      duration: 100,
      repeat: 2,
      yoyo: true,
      ease: Phaser.Math.Easing.Sine.InOut,
      onUpdate: (tween) => {
        const value = tween.getValue();
        const colorObject = Phaser.Display.Color.Interpolate.ColorWithColor(
          startColor,
          endColor,
          100,
          value
        );

        const color = Phaser.Display.Color.GetColor(
          colorObject.r,
          colorObject.g,
          colorObject.b
        );

        this.sprite.setTint(color);
      },
    });

    this.stateMachine.setState("idle");

    this.setHealth(this.health - 15);
  }
```

<p align=center><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/1388ae9e36f64e69af496375ca686843~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=581&h=552&s=2484039&e=apng&f=77&b=537d2f" alt="动画.apng"  /></p>


### 消灭敌人

```js
private snakeStompOnEnter() {
    this.sprite.setVelocityY(-10);

    events.emit("snake-stomped", this.lastSnake);

    this.stateMachine.setState("idle");
}
```

设置 `setVelocityY`可以让熊猫向上移动。由于在熊猫类中，我们没有对应的毒蛇的实例，所以我们通过事件触发。

```js
  private handleStomped(snake: Phaser.Physics.Matter.Sprite) {
    if (this.sprite !== snake) {
      return;
    }

    events.off("snake-stomped", this.handleStomped, this);

    this.scene.tweens.add({
      targets: this.sprite,
      displayHeight: 0,
      y: this.sprite.y + this.sprite.displayHeight * 0.5,
      duration: 200,
      onComplete: () => {
        this.sprite.destroy();
      },
    });

    this.stateMachine.setState("dead");
  }
```

一旦销毁我们第一时间需要考虑到清理事件，然后我们通过 `this.scene.tweens.add` 补间动画。动画属性是 `displayHeight` 和 `y`。完成后执行销毁。

<p align=center><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/f97bbed892db48fcb8d53117d5f2ce6e~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=581&h=552&s=7856245&e=apng&f=99&b=376f28" alt="动画.apng"  /></p>


### 控制敌人状态

```js
import StateMachine from "../statemachine/StateMachine";
import { sharedInstance as events } from "./EventCenter";

export default class SnakeController {
  private scene: Phaser.Scene;
  private sprite: Phaser.Physics.Matter.Sprite;
  private stateMachine: StateMachine;

  private moveTime = 0;

  constructor(scene: Phaser.Scene, sprite: Phaser.Physics.Matter.Sprite) {
    this.scene = scene;
    this.sprite = sprite;

    this.createAnimations();

    this.stateMachine = new StateMachine(this, "snake");
    // 初始化状态机
    this.stateMachine
      .addState("idle", {
        onEnter: this.idleOnEnter,
      })
      .addState("move-left", {
        onEnter: this.moveLeftOnEnter,
        onUpdate: this.moveLeftOnUpdate,
      })
      .addState("move-right", {
        onEnter: this.moveRightOnEnter,
        onUpdate: this.moveRightOnUpdate,
      })
      .addState("dead")
      .setState("idle");

    events.on("snake-stomped", this.handleStomped, this);
  }

  destroy() {
    events.off("snake-stomped", this.handleStomped, this);
  }

  update(dt: number) {
    this.stateMachine.update(dt);
  }
    
  // 生成精灵图
  private createAnimations() {
    this.sprite.anims.create({
      key: "idle",
      frames: [{ key: "snake", frame: "slug_1.png" }],
    });

    this.sprite.anims.create({
      key: "move-left",
      frames: this.sprite.anims.generateFrameNames("snake", {
        start: 1,
        end: 3,
        prefix: "slug_",
        suffix: ".png",
      }),
      frameRate: 5,
      repeat: -1,
    });

    this.sprite.anims.create({
      key: "move-right",
      frames: this.sprite.anims.generateFrameNames("snake", {
        start: 1,
        end: 3,
        prefix: "slug_",
        suffix: ".png",
      }),
      frameRate: 5,
      repeat: -1,
    });
  }

  private idleOnEnter() {
    this.sprite.play("idle");
    const r = Phaser.Math.Between(1, 100);
    if (r < 50) {
      this.stateMachine.setState("move-left");
    } else {
      this.stateMachine.setState("move-right");
    }
  }

  private moveLeftOnEnter() {
    this.moveTime = 0;
    this.sprite.play("move-left");
  }

  private moveLeftOnUpdate(dt: number) {
    this.moveTime += dt;
    this.sprite.setVelocityX(-3);
    // 由于精灵图是一个朝向，当我们键盘按的是反方向我们可以直接设置 flipX
    this.sprite.flipX = false;
    
    // 超过2s自动反方向
    if (this.moveTime > 2000) {
      this.stateMachine.setState("move-right");
    }
  }

  private moveRightOnEnter() {
    this.moveTime = 0;
    this.sprite.play("move-right");
  }

  private moveRightOnUpdate(dt: number) {
    this.moveTime += dt;
    this.sprite.setVelocityX(3);
    this.sprite.flipX = true;

    if (this.moveTime > 2000) {
      this.stateMachine.setState("move-left");
    }
  }

  private handleStomped(snake: Phaser.Physics.Matter.Sprite) {
    if (this.sprite !== snake) {
      return;
    }
    events.off("snake-stomped", this.handleStomped, this);

    this.scene.tweens.add({
      targets: this.sprite,
      displayHeight: 0,
      y: this.sprite.y + this.sprite.displayHeight * 0.5,
      duration: 200,
      onComplete: () => {
        this.sprite.destroy();
      },
    });

    this.stateMachine.setState("dead");
  }
}
```

毒蛇的自动巡逻逻辑，是通过 update 生命周期函数的 dt，也就是时间变化的值来修改移动位置，一旦到达几秒后，然后 `setState('move-left')` 反方向移动。



## 优化

在最后环节，我们将介绍 UI 界面的美化、音频等来提升游玩体验，对存在的 bug 如跳跃错误进行修正。 


### UI 界面优化

```js
import Phaser from "phaser";
import { sharedInstance as events } from "./EventCenter";

export default class UI extends Phaser.Scene {
  private starsLabel!: Phaser.GameObjects.Text;
  private starsCollected = 0;
  private graphics!: Phaser.GameObjects.Graphics;

  private lastHealth = 100;

  constructor() {
    super({
      key: "ui",
    });
  }

  init() {
    this.starsCollected = 0;
    events.on("star-collected", this.handleStarCollected, this);
    events.on("health-changed", this.handleHealthChanged, this);

    this.events.once(Phaser.Scenes.Events.SHUTDOWN, () => {
      events.off("star-collected", this.handleStarCollected, this);
      events.off("health-changed", this.handleHealthChanged, this);
    });
  }

  create() {
    this.graphics = this.add.graphics();
    this.setHealthBar(100);

    this.starsLabel = this.add.text(10, 35, "star  0", {
      fontSize: "32px",
      color: "#fff",
      fontFamily: "Arial",
      stroke: "#00ff00", // 绿色外边框颜色
      strokeThickness: 10, // 外边框粗细
      shadow: {
        offsetY: 2, // 阴影纵向偏移
        blur: 4, // 阴影模糊程度
        stroke: true, // 是否为阴影添加外边框
        fill: true, // 是否填充阴影
      },
    });

    this.add.image(0, 0, "star");
  }

  /**
   * 设置健康条
   * @param value
   */
  private setHealthBar(value: number) {
    const width = 120;
    const percent = Phaser.Math.Clamp(value, 0, 100) / 100;

    this.graphics.clear();
    this.graphics.fillStyle(0x808080);
    this.graphics.fillRoundedRect(10, 10, width, 20, 5);
    if (percent > 0) {
      this.graphics.fillStyle(0x6df300);
      this.graphics.fillRoundedRect(10, 10, width * percent, 20, 5);
    }
  }

  /**
   * 设置健康值
   * @param value
   */
  private handleHealthChanged(value: number) {
    this.tweens.addCounter({
      from: this.lastHealth,
      to: value,
      duration: 200,
      ease: Phaser.Math.Easing.Sine.InOut,
      onUpdate: (tween) => {
        const value = tween.getValue();
        this.setHealthBar(value);
      },
    });

    this.lastHealth = value;
  }

  /**
   * 收集星星
   */
  private handleStarCollected() {
    ++this.starsCollected;
    this.starsLabel.text = `star ${this.starsCollected}`;
  }
}
```

在这段代码中我们注册了事件，比如健康值变化、吃到了星星，然后执行 phaser 的内部图形渲染 API 用于界面的更新。

### 音频类

```js
import Phaser from "phaser";
import { sharedInstance as events } from "./EventCenter";

export default class AudioPlay extends Phaser.Scene {
  constructor() {
    super("audio");
  }

  preload() {
    this.load
      .audio("bg", "assets/audio/bg.mp3")
      .audio("hitWall", "assets/audio/hitWall.wav")
      //...
  }
  init() {
    events.on("pause_bg", this.pauseBgMusic, this);
    events.on("play_bg", this.playBgMusic, this);
    events.on("play_hitWall", this.playHitWallMusic, this);
    // ....

    this.events.once(Phaser.Scenes.Events.SHUTDOWN, () => {
      events.off("pause_bg", this.pauseBgMusic, this);
      events.off("play_bg", this.playBgMusic, this);
      events.off("play_hitWall", this.playHitWallMusic, this);
      // ....
    });
  }

  create() {
    const bg = this.sound.add("bg", { loop: true });
    this.sound.add("bg");
    this.sound.add("hitWall");
    bg.play();
  }

  pauseBgMusic() {
    this.sound.get("bg").pause();
  }
  // ....
}

```

我们在游戏开始的时候，默认开启背景音乐，在 init 中监听事件，一旦监听到`Phaser.Scenes.Events.SHUTDOWN` 会触发场景的关闭事件，通过监听这个事件来执行一些清理操作或处理场景关闭时的逻辑。


### 多次跳跃错误

<p align=center><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/3393eaf428de4949968add228bca99f7~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=582&h=526&s=6880658&e=apng&f=58&b=418633" alt="动画.apng"  /></p>

实际玩的过程中，我们发现如果连续点击空格跳跃，会导致一直在空中悬挂。这是因为我们的跳跃事件一直没有被切换，多次触发一直都是 jump 跳跃事件。这个时候我们要获取熊猫精灵的位置信息，一旦向下代表坠落就无法触发 jump 事件。


```js
  update(dt: number) {
    this.stateMachine.update(dt);
    // 物体的y轴速度发生了变化
    const currentVelocityY = this.sprite.body.velocity.y;
    if (currentVelocityY > 0) {
      this.status = "drop";
    } else if (currentVelocityY < 0) {
      this.status = "jump";
    } else {
      this.status = "";
    }
  }

  private jumpOnEnter() {
    if (this.status === "drop") return;
    this.sprite.setVelocityY(-12);
  }
```
  

## 游戏演示

<p align=center><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/5ac33e279f2144bfab9a4ad6844170f2~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=518&h=408&s=18214034&e=apng&f=219&b=2a5b20" alt="动画.apng"  /></p>

<p align=center><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/87d17c8d777246a2b59a4704e57436db~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=582&h=526&s=11179096&e=apng&f=134&b=3b722a" alt="动画.apng"  /></p>

到这里，我们就完成了这个小游戏：玩家需要躲避敌人，获取钥匙，然后打开通道，并逃出森林之门。当然，你也可以根据思路自行实现游戏逻辑和深入优化，欢迎在评论区分享。



## 总结

最后，我还是通过几个问题来总结内容，希望你能回顾整篇文章、有所思考并拓展。

- 动作的状态机如何实现？
- 不同类中我们是通过哪些方法通信的？请说出 3 种。
- 我们是如何防止多次跳跃的 bug 的？
- 我们是如何构建 UI 的？
- 我们是如何切换场景对象的？
- 你学会了实时用 Tiled 更新本地地图吗？

如果你理解了这些，恭喜你已经学到了 Phaser 的精髓！后续的游戏开发之路也会走得更加平坦！