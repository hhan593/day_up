
本文我们将通过 CSS 一起来实现：

- 2 维图表：柱状图、饼图、圆环图、散点图、折线图；  
- 3 维图表：3D 柱状图。


## CSS 实现柱状图

<p align=center><img src="https://p1-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/e8f28fc3e110409191409e8ead29ce03~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=425&h=382&s=9019&e=png&b=f2f2f2" alt="image.png"  /></p>

使用 CSS 实现这个图表非常简单，我们只要考虑每个柱子做百分比的计算，同时通过 flex 布局快速排布；为了更加简洁地生成 label，我们通过伪元素和 HTML 的自定义属性、 CSS 的 attr 属性来快速生成文案。

代码如下：

```html
<style>
    .chart {
        width: 300px;
        height: 300px;
        display: flex;
        justify-content: space-between;
        align-items: flex-end;
        background-color: #f0f0f0;
        padding: 10px;
        position: relative;
    }

    .bar {
        width: 30px;
        background-color: #3498db;
        transition: height 0.5s ease-in-out;
        position: relative;
    }

    .bar::after {
        content: attr(data-value);
        position: absolute;
        bottom: 100%;
        left: 50%;
        transform: translateX(-50%);
        font-size: 12px;
        color: #333;
    }
</style>
<body>
    <div class="chart">
        <div class="bar" style="height: 80%" data-value="80"></div>
        <div class="bar" style="height: 60%" data-value="60"></div>
        <div class="bar" style="height: 40%" data-value="40"></div>
        <div class="bar" style="height: 20%" data-value="20"></div>
    </div>
</body>
```


## CSS 实现饼图和圆环图

关于`饼图`的实现方法，这里会介绍两种实现思路：**一种是 `Clip 裁剪 + transform 变换`，另一种是通过`渐变`来实现**。

另外，需要注意的是，`圆环图`的实现是基于饼图的，我们可以在饼图中心覆盖一层圆形，这样就形成了圆环图。

### 1. Clip 裁剪 + Transform 变换

核心思路：首先我们要通过绝对定位来做分层，每一层都是一个正方形，每个正方形通过 Transform 来做旋转的定位，然后做对应的裁剪即可。

代码如下：

```html
<style>
    .pieContainer {
        height: 150px;
        position: relative;
    }

    .pieBackground {
        position: absolute;
        width: 150px;
        height: 150px;
        border-radius: 100%;
        box-shadow: 0px 0px 8px rgba(0, 0, 0, 0.5);
    }

    .pie {
        transition: all 1s;
        position: absolute;
        width: 150px;
        height: 150px;
        border-radius: 100%;
        clip: rect(0px, 75px, 150px, 0px);
    }

    .hold {
        position: absolute;
        width: 150px;
        height: 150px;
        border-radius: 100%;
        clip: rect(0px, 150px, 150px, 75px);
    }

    #pieSlice1 .pie {
        background-color: #1b458b;
        transform: rotate(30deg);
    }

    #pieSlice2 {
        transform: rotate(30deg);
    }

    #pieSlice2 .pie {
        background-color: #0a0;
        transform: rotate(60deg);
    }

    #pieSlice3 {
        transform: rotate(90deg);
    }

    #pieSlice3 .pie {
        background-color: #f80;
        transform: rotate(120deg);
    }

    #pieSlice4 {
        transform: rotate(210deg);
    }

    #pieSlice4 .pie {
        background-color: #08f;
        transform: rotate(10deg);
    }

    #pieSlice5 {
        transform: rotate(220deg);
    }

    #pieSlice5 .pie {
        background-color: #a04;
        transform: rotate(70deg);
    }

    #pieSlice6 {
        transform: rotate(290deg);
    }

    #pieSlice6 .pie {
        background-color: #ffd700;
        transform: rotate(70deg);
    }

</style>
<div class="pieContainer">
    <div class="pieBackground"></div>
    <div id="pieSlice1" class="hold">
        <div class="pie"></div>
    </div>
    <div id="pieSlice2" class="hold">
        <div class="pie"></div>
    </div>
    <div id="pieSlice3" class="hold">
        <div class="pie"></div>
    </div>
    <div id="pieSlice4" class="hold">
        <div class="pie"></div>
    </div>
    <div id="pieSlice5" class="hold">
        <div class="pie"></div>
    </div>
    <div id="pieSlice6" class="hold">
        <div class="pie"></div>
    </div>
    <div class="innerCircle">
    </div>
</div>
```

实现效果如下：


<p align=center><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/ce385ead58ec49daa7f4e77e61634bef~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=591&h=491&s=48519&e=png&b=fbfbfb" alt="image.png"  /></p>


这里可能有小伙伴会问了：`clip` 和 `clip-path` 的区别是什么？

我们这里使用 clip，其实 clip 已经被 CSS3 弃用，在使用 clip 裁剪的时候我们必须使用绝对定位，但在 clip-path 中我们不需要绝对定位，就可以裁剪出圆形、椭圆等形状。

```css
/* 裁剪矩形 */
clip-path: inset(10px 20px 30px 40px);
/* 裁剪多边形, polygon的每组代表每个点的坐标百分比位置 */
clip-path: polygon(50% 0%, 90% 20%, 100% 60%, 75% 100%, 25% 100%, 0% 60%, 10% 20%);

position: absolute;
clip: rect(110px, 170px, 160px, 60px);
```

对于 clip 中的参数，`rect()` 中代表矩形的坐标参数依次表示上边界、右边界、下边界和左边界，就像下图生动地做了描述：

<p align=center><img src="https://p9-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/689d2cf986d44d99a2af025eaa8a6a50~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=398&h=253&s=190209&e=png&b=666463" alt="image.png"  /></p>

既然知道了 clip-path 的好处，那么接下来我们做个替换：

```css
clip: rect(0px, 75px, 150px, 0px);
->
clip-path: polygon(0% 0%, 50% 0%, 50% 100%, 0% 100%);
```

为什么需要裁剪两次呢？

因为`.pie` 类的剪切，将元素的左半部分剪切掉，只显示右半部分。这样一来，`.pie` 元素就变成了一个半圆形。`.hold` 类的剪切将元素的右半部分剪切掉，只显示左半部分。这样一来，`.hold` 元素就变成了一个与 `.pie` 元素相对应的半圆形。这么做就是因为矩形裁剪是单方向的，一旦圆环超过半圆就无法裁剪了。所以需要裁剪两次。

为了实现自定义数据渲染，接着我们需要抽离变量，比如半径和宽度，每一个环的旋转半径我们通过 DOM 的自定义属性获取。

```css
:root {
    --w: 150px;
    --r: 15px;
}

.pieContainer {
    height: var(--w);
    position: relative;
}

.pieBackground {
    position: absolute;
    width: var(--w);
    height: var(--w);
    border-radius: 100%;
    box-shadow: 0px 0px 8px rgba(0, 0, 0, 0.5);
}
// ...
```


接下来我们需要通过动态数据来渲染视图层：


```html
<script>
    const option = {
        data: [
            {
                name: '苹果',
                val: 10,
            },
            {
                name: '橘子',
                val: 20,
            },
            {
                name: '香蕉',
                val: 5,
            },
            {
                name: '香梨',
                val: 15,
            },
            {
                name: '西瓜',
                val: 20,
            },
            {
                name: '菠萝',
                val: 30,
            }
        ]
    }

    const pieSlices = document.querySelectorAll('.hold');
    const pie = document.querySelectorAll('.pie');

    // 计算百分比val
    option.data.forEach(item => {
        item.val = (item.val / 100) * 360
    })
    let firstVal = option.data[0].val

    // 计算每个pie的旋转角度
    pieSlices.forEach((slice, index) => {
        const angle = option.data[index].val; // 计算角度
        pie[index].style.transform = `rotate(${angle}deg)`;
    });

    // 计算每个pie之前定位的旋转角度
    option.data.forEach((item, index) => {
        if (index > 0) {
            const sum = firstVal + item.val
            if (pieSlices[index + 1]) {
                pieSlices[index + 1].style.transform = `rotate(${sum}deg)`;
                firstVal = sum
            }

        } else {
            pieSlices[1].style.transform = `rotate(${firstVal}deg)`;
        }
    })
</script>
```

想要实现`圆环图`，我们在之前的基础上做个遮罩即可：


```html
<style>
.innerCircle {
    position: absolute;
    width: 120px;
    height: 120px;
    background-color: #ffffff;
    border-radius: 100%;
    top: 15px;
    left: 15px;
    box-shadow: 0px 0px 8px rgba(0, 0, 0, 0.5) inset;
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
}
</style>
<div class="innerCircle">
</div>
```

实现效果如下：

<p align=center><img src="https://p9-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/f1d386466f93483f9213e8872a1c71e3~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=532&h=506&s=68105&e=png&b=fbfbfb" alt="image.png"  /></p>



### 2. conic-gradient 圆锥渐变

第一种方案由于基于裁剪很显然分割是很明确的，导致我们无法实现渐变，尤其当需求需要渐变表示递进的强弱关系时，我们就可以使用`圆锥渐变`轻松实现。

实现思路：我们通过计算环的周长，总长其实就是 stroke-dasharray，通过 strokeDashoffset 来偏移我们的虚线线段，那么开始的就是我们的实线线段。其实就是一个蚂蚁线。让这个线长度等于我们的环长度，通过 api 让实线在开始的位置。下图白色环状区域就是我们的 strokeDashoffset 的偏移量。最后圆锥渐变可以轻松定义我们的百分比的渐变程度，比如 0 到 70% 是某个颜色，70% 到 100% 是某个颜色，等等。

效果如下：

<p align=center><img src="https://p9-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/40c3619a788c4095a91f7e01720a7742~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=623&h=551&s=42828&e=png&b=fefefe" alt="image.png"  /></p>


实现代码如下：

```html
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<style>
    .circle {
        width: 300px;
        height: 300px;
        background: conic-gradient(#179067, #62e317, #d7f10f, #ffc403, #fcc202, #ff7327, #ff7327, #FF5800, #ff5900, #f64302, #ff0000, #ff0000);
        border-radius: 50%;
        position: relative;
    }

    #progress-circle circle {
        stroke-dasharray: 880;
        stroke: #f2f2f2;
    }

    #progress-circle {
        transform: rotate(-90deg);
    }

    .circle-mask {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        z-index: 2;
        width: 260px;
        height: 260px;
        background: #fff;
        border-radius: 50%;
    }
</style>

<body>
    <div class="circle">
        <svg id="progress-circle" width="300" height="300">
            <circle r="140" cx="150" cy="150" stroke-width="21" fill="transparent" />
        </svg>
        <div class="circle-mask"></div>
    </div>
</body>
<script>
    const circle = document.querySelector('#progress-circle circle');
    const radius = circle.r.baseVal.value;
    const circumference = radius * 2 * Math.PI;
    function setProgress(percent) {
        const progress = circumference - (percent / 100) * circumference;
        circle.style.strokeDashoffset = -progress;
    }
    let prog = 40
    let val = 100 - prog
    setProgress(val); //设置初始进度

</script>

</html>
```

圆锥渐变最大的问题就是会有兼容问题，比如在 Firefox 和 Internet Explorer 中会不支持。除此之外，在 Safari 浏览器上会有一个很大的问题，就是 Safari 的 dasharray 的实线和虚线的第一个元素是不是实线，而在 Chrome 中第一个是虚线，这里就需要做个简单的兼容了。

```js
/**
 *设置百分比
 * @param percent 偏移白色环状的量
 */
function setProgress(percent) {
    const progress = (percent / 100) * innerCircumference
    const userAgent = navigator.userAgent
    if (userAgent.indexOf('Safari') > -1 && userAgent.indexOf('Chrome') < 0) {
        circle.style.strokeDashoffset = (outerCircumference + (outerCircumference - progress)) + ''
    } else {
        circle.style.strokeDashoffset = -(progress) + ''
    }
}
```

## CSS 实现散点图、折线图

实现`散点图`的核心思路在于计算 x、y 的坐标，`折线图`的实现是在散点图的基础上通过三角函数来得到角度值，最后通过 transform 的 rotate 旋转。

```js
<div class="css-chart" style="--size: 200px;">
    <ul class="line-chart">
        <li>
            <div class="data-point" data-value="10" style="bottom: 20px; left: 40px;"></div>
        </li>
        <li>
            <div class="data-point" data-value="20" style="bottom: 40px; left: 80px;"></div>
        </li>
        <li>
            <div class="data-point" data-value="40" style="bottom: 80px; left: 120px;"></div>
        </li>
        <li>
            <div class="data-point" data-value="20" style="bottom: 40px; left: 160px;"></div>
        </li>
        <li>
            <div class="data-point" data-value="10" style="bottom: 20px; left: 200px;"></div>
        </li>
    </ul>
</div>

<style>
    .css-chart {
        /* 模拟坐标系 */
        border-bottom: 1px solid;
        border-left: 1px solid;
        /* 在 html 中定义宽高 */
        height: var(--size);
        width: var(--size);

        margin: 1em;
        padding: 0;
        position: relative;
    }

    .line-chart {
        list-style: none;
        margin: 0;
        padding: 0;
    }

    .data-point {
        background-color: rgb(14, 41, 163);
        border: 2px solid rgb(14, 41, 163);
        border-radius: 50%;
        width: 10px;
        height: 10px;
        position: absolute;
        transform: translateX(-5px);
    }
</style>
```

我们直接在行内样式 --size 来控制图表宽高，通过定位 bottom、left 来控制对应图表的 x、y 坐标。注意：拐点本身的宽高我们需要用`translateX`去掉宽度的一半。

<p align=center><img src="https://p6-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/0d20c4e4b5674990accfd944b5647c0b~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=319&h=280&s=3428&e=png&b=ffffff" alt="image.png"  /></p>

这个时候就已经实现了 CSS 散点图的核心，那么如何实现点与点的连接呢？

答案是通过**三角函数**。我们首先获得当前点和下一个点的宽度连线，也就是邻边。通过邻边和对边的勾股定理能计算出斜边的长度。而我们知道 `sin（x）= 对边 / 斜边`，然后通过该公式 `var angle = Math.acos(对边 / 斜边) * (180 / Math.PI);` 我们可以直接得到**角度值**。

```js
<div class="css-chart" style="--size: 200px;">
    <ul class="line-chart">
        <li style="--y: 20px; --x: 40px">
            <div class="data-point" data-value="10"></div>
            <div class="line-segment" style="--hypotenuse: 44.72;--angle: 63.43; "></div>
        </li>
        <li style="--y: 40px; --x: 80px">
            <div class="data-point" data-value="20"></div>
            <div class="line-segment" style="--hypotenuse: 56.56;--angle: 45;"></div>
        <li style="--y: 80px; --x: 120px">
            <div class="data-point" data-value="40"></div>
            <div class="line-segment" style="--hypotenuse: 56.56;--angle: 135;"></div>
        </li>
        <li style="--y: 40px; --x: 160px">
            <div class="data-point" data-value="20"></div>
            <div class=" line-segment" style="--hypotenuse: 44.72;--angle: 116.56;"></div>
        </li>
        <li style="--y: 20px; --x: 200px">
            <div class="data-point" data-value="10"></div>
        </li>
    </ul>
</div>

<style>
    .css-chart {
        /* 模拟坐标系 */
        border-bottom: 1px solid;
        border-left: 1px solid;
        /* 在 html 中定义宽高 */
        height: var(--size);
        width: var(--size);

        margin: 1em;
        padding: 0;
        position: relative;
    }

    .line-chart {
        list-style: none;
        margin: 0;
        padding: 0;
    }

    .data-point {
        background-color: rgb(14, 41, 163);
        border: 2px solid rgb(14, 41, 163);
        border-radius: 50%;
        width: 10px;
        height: 10px;
        z-index: 999;
        position: absolute;
        transform: translate(-5px, 5px);

        bottom: var(--y);
        left: var(--x);
    }

    .line-segment {
        background-color: red;
        bottom: var(--y);
        height: 3px;
        left: var(--x);
        position: absolute;
        transform-origin: left bottom;
        width: calc(var(--hypotenuse) * 1px);
        transform: translate(1.5px, -1.5px) rotate(calc((var(--angle) - 90) * 1deg));
    }
</style>
```

<p align=center><img src="https://p6-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/7862f17c3cb1469f939558dea58fd297~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=292&h=254&s=3791&e=png&b=ffffff" alt="image.png"  /></p>

另外，我们可以通过 js 计算每个点的坐标，动态修改行内样式的 CSS 变量，来实现动态图表的展示，包括 label、tooltip、axis 等交互实现。（具体代码就不做演示了。）




## CSS  实现 3D 柱状图

我们来看个示例图：

<p align=center><img src="https://p6-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/a296d6d65ada4adda6b0084bc975ed8d~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1616&h=631&s=82109&e=png&b=f1f1f1" alt="image.png"  /></p>

实现上面的柱状图，首先肯定要用 CSS 的 perspective，接下来我们就从最简单的实现一个立方体开始。


```css
:root {
    --recW: 200px;
    --recH0: 0;
    --recH1: 0;
    --recH2: 0;
    --recH3: 0;
}

/* 定义立方体 */
.cube {
        width: 200px;
        height: 0;
        transform-style: preserve-3d;
        transform: perspective(12000px) translateY(400px) rotateY(-10deg);
}

.face {
    position: absolute;
    width: var(--recW);
    opacity: 0.9;
    text-align: center;
    line-height: 400px;
    font-size: 28px;
}

.c1 .face {
    height: var(--recH0);
}

.c1 {
    transform: translateY(calc(var(--recH0) * -1));
}

.front {
        background-color: #ff0606;
        transform: translateZ(calc(var(--recW) / 2));
        display: flex;
        justify-content: center;
        align-items: center;
}

.back {
    background-color: #700909;
    transform: translateZ(calc(var(--recW) / 2 * -1)) rotateY(180deg);
}

.left {
    background-color: #700909;
    transform: translateX(calc(var(--recW) / 2 * -1)) rotateY(-90deg);
}

.right {
    background-color: #700909;
    transform: translateX(calc(var(--recW) / 2)) rotateY(90deg);
}

.top {
    background-color: #ff3e3e;
    transform: translateY(calc(var(--recW) / 2 * -1)) rotateX(90deg);
}
```


这里需要注意下这段 CSS `transform: perspective(12000px) translateY(400px) rotateY(-10deg);`，因为我们的默认视点是屏幕中心，这就意味着我们想看到立方体的顶面，就必须设置`translateY()`，为了展示右侧面我们必须设置`rotateY()`，这个时候一定是一个透视视图而不是正交视图，所以我们将设置 perspective 为一个非常大的值，来矫正成一个正交的视图。如果不设置 perspective，没有透视效果，意味着你的柱子是会看不到顶面的。

我们将上面的 CSS 定义每个面，并修改面的颜色和位置，同时每个面内部的元素都会跟随面的透视。下面是具体的 dom 布局结构：

```js
 <div class="c1">
    <div class="cube">
        <div class="face front">
        </div>
        <div class="face back"></div>
        <div class="face left"></div>
        <div class="face right"></div>
        <div class="face2 top"></div>
    </div>
    // 定义多个cube
</div>
```


有个立方体后，我们接着需要定义多个，这里用 js 简单生成主体，通过 CSS 的变量来改变面的值，同时考虑交互的效果增加 hover 动态的类名。这里需要考虑不做无用面的渲染，比如底面 Bottom。可以看到在 top 面上我们重新定义了一个 face2，因为只有顶面和底面是宽度不一致的，需要单独控制。

顶面的 CSS 定义：

```css
.face2 {
    position: absolute;
    width: var(--recW);
    height: 200px;
    opacity: 0.9;
    text-align: center;
}
```

js 代码如下：

```js
<script>
        const config = {
            xAxis: ["2040Q1", '2040Q2', '2040Q3', '2040Q3'],
            yAxis: ["400亿", "300亿", "200亿", "100亿", "0亿"],
            maxY: 400,
            data: [
                {
                    quarter: 'Q1',
                    val: 200,
                },
                {
                    quarter: 'Q2',
                    val: 100,
                },
                {
                    quarter: 'Q3',
                    val: 300,
                },
                {
                    quarter: 'Q4',
                    val: 50,
                }
            ]
        }
        const cubes = document.querySelectorAll('.cube');
        const maxId = config.data.findIndex(item => item.val === Math.max(...config.data.map(item => item.val)))

        cubes[maxId].className = 'cube cube-max'
        
        // 设置每个柱子的高度
        const root = document.documentElement;

        config.data.forEach((item, index) => {
            const heightPercentage = item.val
            root.style.setProperty(`--recH${index}`, `${heightPercentage}px`);
        });
        // 设置 val
        const frontElements = document.querySelectorAll('.front');
        config.data.forEach((item, index) => {
            frontElements[index].innerHTML = `<div class='front-txt'>${item.val}亿</div>`;
        })
        // 设置 xAxis 的值
        const xAxisElements = document.querySelectorAll('.xAxis > div');
        for (let i = 0; i < config.xAxis.length; i++) {
            xAxisElements[i].textContent = config.xAxis[i];
        }

        // 设置 yAxis 的值
        const yAxisElements = document.querySelectorAll('.yAxis > div');
        for (let i = 0; i < config.yAxis.length; i++) {
            yAxisElements[i].textContent = config.yAxis[i];
        }

    </script>
```


## 创建背景立方体和坐标轴

同样，我们先来看个示例图：

<p align=center><img src="https://p6-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/e81de9989ce546c497c49528cf99205e~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1585&h=628&s=36248&e=png&b=fafafa" alt="image.png"  /></p>

其实这只是个立方体，正面和右面是一个透明的面，我们可以根据之前的做法再实现一个，这里需要注意背景立方体的 DOM 位置需要在 4 个立方体的上面。它会直接影响绘图顺序。

```html
 <div class="bg-cube">
    <!-- y轴 -->        
    <div class="yAxis">
        <div></div>
        <div></div>
        <div></div>
        <div></div>
    </div>
    <!-- x轴 -->
    <div class="xAxis">
        <div></div>
        <div></div>
        <div></div>
        <div></div>
    </div>
    <div class="bg-face bg-front">
    </div>
    <div class="bg-face bg-back"></div>
    <div class="bg-face bg-left"></div>
    <div class="bg-face2 bg-bottom"></div>
</div>
```

同时在面上的元素，我们可以直接用**绝对定位设置元素**，而不用在父类设置 relative，比如可以绘制坐标轴，我们在 `bg-cube`下面定义了 yAxis 和 xAxis。

```css
.xAxis {
    position: absolute;
    left: -140px;
    top: 450px;
    width: 100%;
    font-size: 28px;
    display: flex;
    text-align: right;
}

.xAxis div {
    flex: 1;
    text-align: center;
}

.yAxis {
    position: absolute;
    top: 20px;
    left: -230px;
    font-size: 28px;
    height: 420px;
    display: flex;
    flex-direction: column;
    text-align: right;
}

.yAxis div {
    flex: 1;
}
```

由于之前为了正交效果我们设置了`translateY(400px) `，所以我们需要根据高度做反方向偏移，在每个 cube 上面我们都要设置 c1、c2、c3、c4 来反方向移动。

```js
.c1 {
    transform: translateY(calc(var(--recH0) * -1));
}

.c2 {
    transform: translateX(300px) translateY(calc(var(--recH1) * -1));
}

.c3 {
    transform: translateX(600px) translateY(calc(var(--recH2) * -1));
}

.c4 {
    transform: translateX(900px) translateY(calc(var(--recH3) * -1));
}
```

接下来我们设置下动态交互效果：


```css
   .cube:hover .front,
    .cube:hover .back,
    .cube:hover .left,
    .cube:hover .right,
    .cube:hover .top,
    .cube:hover .bottom {
        opacity: .8;
        filter: blur(.7);
        transition: .2s all linear;
        cursor: pointer;
        color: #fff;
    }

    .cube:hover .front-txt {
        display: block;
    }

    .cube-max .front-txt {
        display: block !important;
        color: #fff;
    }

    .cube-max {
        display: block !important;
    }

    .cube:hover .front,
    .cube:hover .back,
    .cube:hover .left {
        background-color: #3a1cbd;
    }

    .cube:hover .right {
        background-color: #140263;
    }

    .cube:hover .top,
    .cube:hover .bottom {
        background-color: #866bff;
    }

    .front .front-txt {
        display: none;
    }
```

效果如下：

<p align=center><img src="https://p6-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/28f9c0c81c4a42a8a66a4c204bde2a75~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1241&h=542&s=80052&e=gif&f=39&b=f4f4f4" alt="动画.gif"  /></p>

[jcode](https://code.juejin.cn/pen/7314684834066989110)

## 总结

我们总结下各个图表用 CSS 实现的思路：

- **柱状图**：通过 flex 布局 + label（attr 属性 + 伪元素 + html 自定义属性）。
- **饼图和圆环图**： 一种是 `Clip 裁剪 + transform 变换`，另一种是通过`conic-gradient` 渐变来实现。
- **散点图**：图表定位元素，元素根据 bottom、left 对应 x、y 坐标做对应偏移。
- **折线图**：核心在于`var angle = Math.acos(对边 / 斜边) * (180 / Math.PI);` 的计算角度值并 rotate 即可。
- **3D 柱状图**：通过 tranfrom 的 perspective 绘制。

