## 1. 前言

对于一个技术博客而言，SEO 非常重要，好的 SEO 会为你带来更多的流量。

可是问题在于，SEO 要做的事情非常多，一时间竟不知道如何开始优化。

所以本篇我们就梳理下 SEO 要做的事情。

## 2. 开发时注意

### 2.1. 尽可能使用服务端渲染

尽管搜索引擎也开始支持 JS 生成的内容，但还是尽可能使用服务端渲染，将主体内容渲染成 HTML 返回给爬虫

### 2.2. 尽可能使用 Image 组件

尽可能使用 Astro 原生的 `<Image />` 和 `<Picture />` 组件来优化图像，这些组件本身有尺寸优化、懒加载等功能，而且为了维持视觉稳定，它会在图片加载的时候自动阻止布局偏移，此外使用 Image 组件需要设置 alt 属性，这都有利于 SEO。

像谷歌公开表示过，CLS （Cumulative Layout Shift，累计布局偏移，网页整个生命周期内发生的所有意外布局偏移的得分总和）是一个重要指标。

简单来说，就是谷歌要求网页元素在加载期间不要“乱动”，比如点赞按钮本应该在下方，但因为文章还没有加载完毕，于是呈现在上方，文章加载完毕后，就被“挤”到下方，这就很不好。

`<Image />` 组件和 `<Picture />` 组件默认都做了阻止布局偏移处理。

简而言之，优化 Astro 应用程序的性能将直接影响 SEO。

## 3. 添加 robots.txt 和 sitemap.xml

添加站点地图可以使用 [@astrojs/sitemap](https://docs.astro.build/zh-cn/guides/integrations-guide/sitemap/) 集成：

```bash
npx astro add sitemap
```

将站点的 URL 添加到 `astro.config.mjs` 中的 `site` 选项。且这个 URL 必须以 `http://` 或 `https://` 开头，比如：

```javascript
import { defineConfig } from "astro/config";
import sitemap from "@astrojs/sitemap";

export default defineConfig({
  site: "https://yayujs.com",
  integrations: [sitemap()],
  // ...
});
```

> 配置站点地图集成后，在构建站点时，sitemap-index.xml 和 sitemap-0.xml 文件将添加到你的输出目录中。
>
> sitemap-index.xml 链接到所有编号的站点地图文件。 sitemap-0.xml 列出了你网站上的页面。 对于非常大的站点，可能还存在其他编号文件，例如 sitemap-1.xml 和 sitemap-2.xml。

在布局中添加站点地图的链接，让机器人快速找到站点地图：

```html
<head>
  <link rel="sitemap" href="/sitemap-index.xml" />
</head>
```

此外我们还要添加站点地图，有两种方式，一种是使用静态文件，一种是动态生成。

使用静态文件，添加 `public/robots.txt`：

```plain
User-agent: *
Allow: /

Sitemap: https://<YOUR SITE>/sitemap-index.xml
```

使用动态生成，新建`src/pages/robots.txt.ts`，代码如下：

```javascript
import type { APIRoute } from "astro";

const getRobotsTxt = (sitemapURL: URL) => `
User-agent: *
Allow: /
Sitemap: ${sitemapURL.href}
`;

export const GET: APIRoute = ({ site }) => {
  const sitemapURL = new URL("sitemap-index.xml", site);
  return new Response(getRobotsTxt(sitemapURL));
};
```

当部署后，访问 robots.txt：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/24bdc735b96149b09aaf541d9701b21a~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1270\&h=324\&s=38062\&e=png\&b=1b1b1b)

访问站点地图：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/cf240edc7c994edf9b503e5caf5176a0~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1890\&h=424\&s=118185\&e=png\&b=191919)

访问站点地图 `sitemap-0.xml`：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/3c5e10dc792c425ebe42db2c49c99a02~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=3560\&h=920\&s=300483\&e=png\&b=151515)

## 4. 设置每个页面的 Metadata

### 4.1. 基础字段

我们知道，为了方便搜索引擎爬取，每个页面都需要设置 `title`、`description` 等属性，除了这两个属性还有哪些属性要设置呢？

关键看搜索引擎会用到哪些字段，这个可以参考 Google 的官方文档[《Google 支持的 meta 标记和属性》](https://developers.google.com/search/docs/crawling-indexing/special-tags?hl=zh-cn)。简而言之，最常用的就是 description：

```html
<meta name="description" content="A description of the page" />
```

注：keywords 这种属性，Google 已经废弃不用了，参考[《Google 不会将关键字元标记用于网页排名》](https://developers.google.com/search/blog/2009/09/google-does-not-use-keywords-meta-tag?hl=zh-cn)。

### 4.2. 网站规范化字段

除此之外，还应该有：

```html
<link rel="canonical" href="https://example.com/dresses/green-dresses" /> <link rel="alternate" type="application/rss+xml" href="http://example.com/rss.xml" />
```

前者用于网站规范化，参考[《如何使用 rel="canonical" 及其他方法指定规范网址》](https://developers.google.com/search/docs/crawling-indexing/consolidate-duplicate-urls?hl=zh-cn)。后者用于网站的 RSS 订阅（如果有的话）。

### 4.3. Open Graph protocol 字段

再然后要支持 Open Graph protocol，介绍参考[《VuePress 博客之 SEO 优化（四） Open Graph protocol》](https://juejin.cn/post/7073416301720371213)。

就是我们常见的 og: 开头的字段：

```javascript
<meta property="og:url" content="http://www.nytimes.com/2015/02/19/arts/international/when-great-minds-dont-think-alike.html" />
<meta property="og:type" content="article" />
<meta property="og:title" content="When Great Minds Don’t Think Alike" />
<meta property="og:description" content="How much does culture influence creative thinking?" />
<meta property="og:image" content="http://static01.nyt.com/images/2015/02/19/arts/international/19iht-btnumbers19A/19iht-btnumbers19A-facebookJumbo-v2.jpg" />
```

除了基础的 `og:title`、`og:image`、`og:url`，还有一个 `og:type`，需要视页面内容做选择，比如如果是我们的博客页面，自然是选择 og 的 article 类型，它又有些具体的字段比如：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/c210ba96c9d24d528fe3c30190b7c135~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1334\&h=462\&s=281196\&e=png\&b=fefdfd)

最终生成的字段示例如下：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/1066938b988f4574b84fcc643716ec76~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1536\&h=410\&s=186137\&e=png\&b=ffffff)

### 4.4. 其他字段

最后还有一些其他字段，比如 [Twitter Card](https://developer.twitter.com/en/docs/twitter-for-websites/cards/guides/getting-started)，用于在 Twitter 进行分享展示，视情况添加。

至于具体如何实现，我们放到下节和 JSON-LD 一起实现。

## 5. 添加 JSON-LD 数据

英文全称：JavaScript Object Notation for Linked Data，官方地址：[json-ld.org/](https://link.juejin.cn?target=https%3A%2F%2Fjson-ld.org%2F)，简单的来说，就是用来描述网页的类型和内容，方便搜索引擎做展现。

比如如果我们在 Google 搜索 「Chocolate in a mug」，我们会看到这样的搜索结果：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/367b0a8ba36f41538f038340c4fadd03~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1512\&h=551\&s=37186\&e=webp\&b=212327)

搜索结果中显示了评分、多少条评价等内容。我们打开页面，就可以看到搜索展示的内容对应了 `application/ld+json` 中的内容：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/dd78a2607536404ebf21c25a412b7e14~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1512\&h=391\&s=108696\&e=webp\&b=f4f1f0)

注：关于 JSON-LD，参考[《VuePress 博客之 SEO 优化（五）添加 JSON-LD 数据》](https://juejin.cn/post/7073749684656799780)

那就让我们来看看如何设置 Metadata 字段和 JSON-LD 吧。

新建 `src/data/site.json`，代码如下：

```json
{ "title": "冴羽的技术博客", "description": "冴羽的前端技术博客，不止有技术，还有生活", "image": { "src": "https://yayujs.com/_astro/avator.BePvCchA_1x1Arc.webp", "alt": "冴羽的博客" } }
```

这里我们设置了站点相关的信息，可以作为默认值使用。

新建 `src/components/SEO.astro`，代码如下：

```jsx
---
import siteData from "../data/site.json";

const {
  title = siteData.title,
  description = siteData.description,
  url = Astro.url,
  image = siteData.image,
  frontmatter,
} = Astro.props;

function jsonLDGenerator({ type, post, url }: any) {
  if (type === "post") {
    return `<script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "BlogPosting",
        "mainEntityOfPage": {
          "@type": "WebPage",
          "@id": "${url}"
        },
        "headline": "${post.title}",
        "description": "${post.description}",
        "image": "${post.image.src}",
        "author": {
          "@type": "Person",
          "name": "${post.author}",
          "url": "${post.authorUrl}"
        },
        "datePublished": "${post.date}"
      }
    </script>`;
  }
  return `<script type="application/ld+json">
      {
      "@context": "https://schema.org/",
      "@type": "WebSite",
      "name": "${siteData.title}",
      "url": "${import.meta.env.SITE}"
      }
    </script>`;
}

const jsonLD = jsonLDGenerator({
  type: frontmatter ? "post" : "website",
  post: frontmatter,
  url,
});
---

<!-- SEO -->
<title>{title}</title>
<meta name="description" content="{description}" />

<link rel="canonical" href="{url}" />
<link rel="alternate" type="application/rss+xml" href="http://yayujs.com/rss.xml" />

<!-- Open Graph -->
<meta property="og:title" content="{title}" />
<meta property="og:site_name" content="{siteData.title}" />
<meta property="og:description" content="{description}" />
<meta property="og:url" content="{url}" />
<meta property="og:image" content="{image?.src}" />
<meta property="og:image:url" content="{image?.src}" />
<meta property="og:image:secure_url" content="{image?.src}" />
<meta property="og:image:type" content="image/jpeg" />
<meta property="og:image:width" content="500" />
<meta property="og:image:height" content="500" />
<meta property="og:image:alt" content="{image?.alt}" />

<!-- Twitter -->
<meta name="twitter:title" content="{title}" />
<meta name="twitter:description" content="{description}" />
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:image" content="{image?.src}" />
<meta name="twitter:image:alt" content="{title}" />
<meta name="twitter:domain" content="{import.meta.env.SITE}" />

<!-- JSON LD -->
<Fragment set:html="{jsonLD}" />
```

在这里我们设置了所有 SEO 需要的字段，如果相关字段没有传入，则使用站点的信息。

修改布局 `src/layouts/Layout.astro`，添加代码如下：

```diff
---
// ...

import SEO from "@/components/SEO.astro";
const { title, description, image, url, frontmatter } = Astro.props;
---

<!doctype html>
<html lang="zh-hans" class="bg-[var(--page-bg)]">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width,initial-scale=1,viewport-fit=cover"
    />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <meta name="generator" content={Astro.generator} />
+    <SEO {title} {description} {url} {image} {frontmatter} />
    <link rel="sitemap" href="/sitemap-index.xml" />
    // ...
  </head>
  // ...
</html>
```

此时页面默认的字段设置如下：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/a5005e7e01504bf780010d2de37d4f02~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2968\&h=1352\&s=1441461\&e=png\&b=2a2a2a)

如果要设置某个页面的字段，在 Layout 中传入相应的值即可：

```jsx
<Layout title="..." description="..." image={{ src: "...", alt: "..." }}>...
```

## 6. 提交站点

不用等待 Google 慢慢收录，你可以直接将站点提交给 Google、百度等搜索引擎，参考 [《VuePress 博客之 SEO 优化（一） sitemap 与搜索引擎收录》](https://juejin.cn/post/7072291456462880782#heading-7)

## 7. 最后

本篇我们介绍了 Astro SEO 要做的事情，我们将主要的代码放入了组件 SEO.astro 中，不过文章中的代码只是一个示例，具体开发的时候还要根据实际情况进行调整。
