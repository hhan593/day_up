## 1. 前言

目前我们已经完成了网站核心部分的开发，现在我们开始为网站部署做准备。

## 2. SEO

对于博客网站，SEO 非常重要。好的 SEO 将会为我们带来更多的自然流量。

关于 SEO 的介绍和实现 ，参考《理论篇 | SEO 要做哪些事情？》。

### 2.1. 添加 robots.txt 和 sitemap.xml

使用 [@astrojs/sitemap](https://docs.astro.build/zh-cn/guides/integrations-guide/sitemap/) 集成添加站点地图：

```bash
npx astro add sitemap
```

将站点的 URL 添加到 `astro.config.mjs` 中的 `site` 选项：

```diff
export default defineConfig({
+  site: "https://blog-course.vercel.app/",
   integrations: [tailwind(), icon(), mdx(), sitemap()],
   // ...
});
```

注：本篇我们会部署到 Vercel 上 ，假设你的项目名称为 blog-course，最终部署后的地址就是 <https://blog-course.vercel.app/>，当然你也可以使用自己的域名地址，然后解析到 Vercel，本篇后面也会讲到。

修改 `src/layouts/BaseLayout.astro`，添加代码如下，目的在于让机器人快速找到站点地图：

```diff
<meta name="generator" content={Astro.generator} />
+<link rel="sitemap" href="/sitemap-index.xml" />
<script is:inline>
```

新建`src/pages/robots.txt.ts`，代码如下：

```javascript
import type { APIRoute } from "astro";

const getRobotsTxt = (sitemapURL: URL) => `
User-agent: *
Allow: /
Sitemap: ${sitemapURL.href}
`;

export const GET: APIRoute = ({ site }) => {
  const sitemapURL = new URL("sitemap-index.xml", site);
  return new Response(getRobotsTxt(sitemapURL));
};
```

此时访问 <http://localhost:4321/robots.txt>，浏览器效果如下：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/8f689711d9d14b83af72c33c8bf7be13~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1434\&h=316\&s=41724\&e=png\&b=1b1b1b)

`sitemap-index.xml` 会在构建的时候生成，所以直接访问会出现 404。如果本地要查看效果的话，运行 `npm run build`、`npm run preview` 查看效果：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/407ab668be284082b545f363e6a0cc8a~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2052\&h=424\&s=102077\&e=png\&b=191919)

### 2.2. 设置每个页面的 SEO 字段

新建 `src/data/site.json`，代码如下：

```json
{
  "title": "我的技术博客",
  "description": "我的前端技术博客，不止有技术，还有生活",
  "image": {
    "src": "https://yayujs.com/_astro/avator.BePvCchA_1x1Arc.webp",
    "alt": "我的博客"
  }
}
```

这里我们设置了站点相关的信息，可以作为默认值使用。

新建 `src/components/SEO.astro`，代码如下：

```jsx
---
import siteData from "../data/site.json";

export type PageSEO = {
  title?: string;
  description?: string;
  url?: string;
  image?: {
    src: string;
    alt: string;
  },
  frontmatter?: Object;
}

const {
  title = siteData.title,
  description = siteData.description,
  url = Astro.url,
  image = siteData.image,
  frontmatter,
} = Astro.props;

function jsonLDGenerator({ type, post, url }: any) {
  if (type === "post") {
    return `<script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "BlogPosting",
        "mainEntityOfPage": {
          "@type": "WebPage",
          "@id": "${url}"
        },
        "headline": "${post.title}",
        "description": "${post.description}",
        "image": "${post.image?.src}",
        "author": {
          "@type": "Person",
          "name": "${post.author}",
          "url": "${post.authorUrl}"
        },
        "datePublished": "${post.published}"
      }
    </script>`;
  }
  return `<script type="application/ld+json">
      {
      "@context": "https://schema.org/",
      "@type": "WebSite",
      "name": "${siteData.title}",
      "url": "${import.meta.env.SITE}"
      }
    </script>`;
}

const jsonLD = jsonLDGenerator({
  type: frontmatter ? "post" : "website",
  post: frontmatter,
  url,
});
---

<!-- SEO -->
<title>{title}</title>
<meta name="description" content="{description}" />
<link rel="canonical" href="{url}" />

<!-- Open Graph -->
<meta property="og:title" content="{title}" />
<meta property="og:site_name" content="{siteData.title}" />
<meta property="og:description" content="{description}" />
<meta property="og:url" content="{url}" />
<meta property="og:image" content="{image?.src}" />
<meta property="og:image:url" content="{image?.src}" />
<meta property="og:image:secure_url" content="{image?.src}" />
<meta property="og:image:type" content="image/jpeg" />
<meta property="og:image:width" content="500" />
<meta property="og:image:height" content="500" />
<meta property="og:image:alt" content="{image?.alt}" />

<!-- Twitter -->
<meta name="twitter:title" content="{title}" />
<meta name="twitter:description" content="{description}" />
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:image" content="{image?.src}" />
<meta name="twitter:image:alt" content="{title}" />
<meta name="twitter:domain" content="{import.meta.env.SITE}" />

<!-- JSON LD -->
<Fragment set:html="{jsonLD}" />
```

在这里我们设置了所有 SEO 相关的字段，如果相关字段没有传入，则使用站点的信息。

修改 `src/layouts/BaseLayout.astro`，完整代码如下：

```jsx
---
import "@/styles/global.css";
import SEO, { type PageSEO } from "@/components/SEO.astro";

interface Props extends PageSEO {}
---

<!doctype html>
<html lang="zh-hans" class="bg-[var(--page-bg)]">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <meta name="generator" content={Astro.generator} />
    <SEO {...Astro.props} />
    <script is:inline>
      const setTheme = () => {
        const theme = (() => {
          if (
            typeof localStorage !== "undefined" &&
            localStorage.getItem("theme")
          ) {
            return localStorage.getItem("theme");
          }
          if (window.matchMedia("(prefers-color-scheme: dark)").matches) {
            return "dark";
          }
          return "light";
        })();

        if (theme === "light") {
          document.documentElement.setAttribute("data-theme", "light");
        } else {
          document.documentElement.setAttribute("data-theme", "dark");
        }

        window.localStorage.setItem("theme", theme || "light");
      };
      setTheme();

      document.addEventListener("astro:after-swap", setTheme);
    </script>
  </head>
  <body>
    <slot />
  </body>
</html>
```

修改 `src/layouts/Layout.astro`，完整代码如下：

```jsx
---
import BaseLayout from "./BaseLayout.astro";
import { Icon } from "astro-icon/components";
import { cva } from "class-variance-authority";
import ThemeIcon from "@/components/ThemeIcon.astro";
import { Image } from "astro:assets";
import type { PageSEO } from "@/components/SEO.astro";

interface Props extends PageSEO {}

const navList = [
  { title: "首页", mtitle: "首页", href: "/", icon: "home" },
  { title: "技术文章", mtitle: "文章", href: "/columns", icon: "order" },
];

const link = cva("flex flex-row justify-start items-center px-4 py-3 rounded-xl mb-2", {
  variants: {
    intent: {
      default: ["hover:bg-[var(--btn-plain-bg)]", "hover:shadow-2xl", "hover:shadow-[#d4ddff]", "dark:hover:shadow-inherit", "hover:text-[--primary]", "dark:hover:text-inherit"],
      active: ["text-[#fff]", "bg-[var(--primary)]"],
    },
  },
  defaultVariants: {
    intent: "default",
  },
});
---

<BaseLayout {...Astro.props}>
  <div id="page" class="flex min-h-screen overflow-hidden rounded-md text-[var(--text)] md:mx-10 md:mt-14">
    <div class="hidden min-w-64 bg-[var(--side-bg)] md:block">
      <div class="mb-5 flex items-center justify-between px-8">
        <a href="/" class="flex flex-row items-center justify-start py-4">
          <Image src="https://xsgames.co/randomusers/avatar.php?g=pixel" width="256" height="256" alt="" class="mr-4 h-10 w-10 rounded-full" />
          <h1 class="text-lg">我的博客</h1>
        </a>
        <ThemeIcon />
      </div>
      <ul class="mb-5 border-b border-solid border-b-[var(--side-border)] px-8 py-0 pb-5">
        {
          navList.map(({ title, href, icon }) => {
            const isCurrentPage = href == "/" ? Astro.url.pathname === href : Astro.url.pathname.startsWith(href);
            const intent = isCurrentPage ? "active" : "default";
            return (
              <li>
                <a href={href} class={link({ intent })}>
                  <Icon name={icon} class="mr-3" />
                  <span>{title}</span>
                </a>
              </li>
            );
          })
        }
      </ul>
    </div>
    <div class="w-full flex-1 bg-[var(--content-bg)] pb-20">
      <slot />
    </div>
    <div class="hidden min-w-64 bg-[var(--side-bg)] xl:block">
      <slot name="right-sidebar">
    </div>
    <div class="footer-nav fixed bottom-0 z-50 flex w-full items-center justify-around border-t border-t-[var(--nav-border-color)] bg-white p-2 text-sm md:hidden dark:bg-[var(--side-bg)]">
      {
        navList.map(({ mtitle, href, icon }) => {
          const isCurrentPage = href == "/" ? Astro.url.pathname === href : Astro.url.pathname.startsWith(href);
          if (!mtitle) return null;
          return (
            <a href={href} class={`flex flex-col items-center justify-center ${isCurrentPage ? "text-[var(--hilight-color)] dark:text-[#fff]" : ""}`}>
              <Icon name={icon} class="mb-1" />
              <span>{mtitle}</span>
            </a>
          );
        })
      }
    </div>
  </div>
  <div class="footer my-4 text-center text-sm text-[#7e85ad]">
    <a href="https://beian.miit.gov.cn/#/Integrated/index" target="_blank"> 浙ICP备xxxxxx号 </a>
  </div>
</BaseLayout>
```

修改 `src/pages/post/[...slug].astro`，完整代码如下：

```jsx
---
import type { InferGetStaticParamsType, GetStaticPaths } from "astro";
import type { CollectionEntry } from "astro:content";
import { getCollection } from "astro:content";
import dayjs from "dayjs";
import Layout from "@/layouts/Layout.astro";
import Toc from "@/components/Toc.astro";
import "remark-alerts/styles/github-colors-light.css";
import "remark-alerts/styles/github-base.css";

export async function getStaticPaths() {
  const posts: CollectionEntry<"posts">[] = await getCollection("posts");
  return posts.map((post) => ({
    params: { slug: post.slug },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content, remarkPluginFrontmatter, headings } = await post.render();

type Params = InferGetStaticParamsType<typeof getStaticPaths>;

const { slug } = Astro.params as Params;

const lastModified = dayjs(remarkPluginFrontmatter.lastModified).format("YYYY-MM-DD HH:mm:ss");
---

<Layout {...remarkPluginFrontmatter} frontmatter={remarkPluginFrontmatter}>
  <div class="prose max-w-none p-10 dark:prose-invert">
    <Content />
    <p>文章最后修改时间：{lastModified}</p>
  </div>
  <Toc {headings} slot="right-sidebar" />
</Layout>

<style is:global>
  /* 自定义 Markdown 代码块样式 */
  @import url("@/styles/markdown.css");

  /* Markdown 插件 katex */
  @import url("https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.css");

  /* Markdown 插件 rehypeAutolinkHeadings */
  @import url("@/styles/anchor.css");

  /* Markdown 插件 rehypeColorChips */
  @import url("@/styles/colorchip.css");

  /* Markdown 插件 remarkAlerts */
  @import url("@/styles/github-alert.css");

  /* Markdown 自定义插件 Github Card */
  @import url("@/styles/github-card.css");

  /* Markdown 复制代码 */
  @import url("@/styles/copy.css");
</style>

<script>
  let copyButtonLabel = "复制代码";
  let codeBlocks = Array.from(document.querySelectorAll("pre"));

  for (let codeBlock of codeBlocks) {
    let wrapper = document.createElement("div");
    wrapper.style.position = "relative";

    let copyButton = document.createElement("button");
    copyButton.className = "copy-code";
    copyButton.innerHTML = copyButtonLabel;

    codeBlock.setAttribute("tabindex", "0");
    codeBlock.appendChild(copyButton);
    codeBlock.parentNode!.insertBefore(wrapper, codeBlock);
    wrapper.appendChild(codeBlock);

    copyButton.addEventListener("click", async () => {
      await copyCode(codeBlock, copyButton);
    });
  }

  async function copyCode(block: HTMLPreElement, button: HTMLButtonElement) {
    let code = block.querySelector("code");
    let text = code!.innerText;

    await navigator.clipboard.writeText(text);

    button.innerText = "复制成功！";

    setTimeout(() => {
      button.innerText = copyButtonLabel;
    }, 700);
  }
</script>
```

修改 `src/content/posts/测试文章1.mdx`的 frontmatter 如下：

```
---
title: "测试文章1"
columns: ["next.js", "javascript"]
published: "2024-09-06 01:00:00"
description: "描述"
image:
  src: "https://placebear.com/200/300"
  alt: "bear"
author: "yayu"
authorUrl: "https://yayujs.com"
---
```

浏览器效果渲染的 SEO 数据如下：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/e8eb6847025b4677aafcaf5e17f23ed0~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=3202\&h=1666\&s=769992\&e=png\&b=282828)

对于其他非文章页面，则根据使用 `<Layout>` 组件时传入的值：

```jsx
<Layout title="技术文章" description="技术文章专栏" image={{src: "https://placebear.com/200/300", alt: "bear" }}>
```

当然这些 props 也可以不传，默认使用 `site.json` 中的数据。

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/6c6651afdced4a38b2eef893e7a49489~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=3162\&h=1140\&s=566033\&e=png\&b=282828)

## 3. RSS 订阅

Astro 提供了 RSS 处理包 [@astrojs/rss](https://github.com/withastro/astro/tree/main/packages/astro-rss)，安装依赖项：

```bash
npm install @astrojs/rss markdown-it sanitize-html
```

新建 `src/pages/rss.xml.js`，完整代码如下：

```javascript
import rss from "@astrojs/rss";
import { getCollection } from "astro:content";
import dayjs from "dayjs";
import customParseFormat from "dayjs/plugin/customParseFormat";
import sanitizeHtml from "sanitize-html";
import MarkdownIt from "markdown-it";
import siteData from "@/data/site.json";
const parser = new MarkdownIt();

dayjs.extend(customParseFormat);
export async function GET(context) {
  const posts = await getCollection("posts");
  return rss({
    title: siteData.title,
    description: siteData.description,
    site: context.site,
    trailingSlash: false,
    items: posts
      .sort((a, b) => Date.parse(b.data.published) - Date.parse(a.data.published))
      .map(({ data: { published, title, description }, slug, body }) => ({
        title,
        ...(description && { description }),
        link: `/post/${slug}/`,
        pubDate: dayjs(`${published}+08:00`, "YYYY-MM-DD HH:mm:ssZ").toDate(),
        // 注意：这不会处理 MDX 文件中的组件或 JSX 表达式。
        content: sanitizeHtml(parser.render(body), {
          allowedTags: sanitizeHtml.defaults.allowedTags.concat(["img"]),
        }),
      })),
  });
}
```

修改 `src/components/SEO.astro`，添加如下代码：

```jsx
<link rel="alternate" type="application/rss+xml" title={siteData.title} href={new URL("rss.xml", Astro.site)} />
```

关于为什么修改这些文件，代码为什么要这样写，参考《理论篇 | RSS 订阅》。

## 4. 预加载

开启预加载，修改 `astro.config.mjs`，添加如下代码：

```diff
export default defineConfig({
  site: "https://blog-course.vercel.app/",
  integrations: [tailwind(), icon(), mdx(), sitemap()],
+  prefetch: {
+    prefetchAll: true,
+  }
});
```

这步是为了做性能优化。性能优化的更多工作应该是在开发中完成的，具体参考《理论篇 | 性能优化要做哪些事情？》

## 5. 部署 Vercel

为了方便管理和部署，我们将代码托管到 Vercel 上。

### 5.1. 安装集成

安装 Vercel 集成：

```bash
npx astro add vercel
```

安装 Vercel 集成默认的输出模式是 `server`：

```diff
import { defineConfig } from 'astro/config';
+import vercel from '@astrojs/vercel/serverless';

export default defineConfig({
  // ...
+  output: 'server',
  adapter: vercel(),
});
```

不过就我们目前的实现而言，并没有使用服务端渲染的必要。将代码 `astro.config.mjs`修改为：

```diff
import { defineConfig } from 'astro/config';
-import vercel from '@astrojs/vercel/serverless';
+import vercel from "@astrojs/vercel/static";

export default defineConfig({
  // ...
-  output: 'server',
  adapter: vercel(),
});
```

这样默认整个网站将在构建的时候进行预渲染。

### 5.2. 部署 Vercel

部署前先本地运行 `npm run build`看看是否能够正确构建，否则提交到了 Vercel，部署时也会报错：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/73db63b4e60b4bbc94dfe2eaccbd770b~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1544\&h=596\&s=139115\&e=png\&b=1e1e1e)

然后我们在 GitHub 上新建一个仓库：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/dc3f8cfa4a48454b8478e7377050ce5f~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=3242\&h=1454\&s=346606\&e=png\&b=0d1116)

新建后就可以看到添加已有仓库会用到的命令：

```bash
git remote add origin git@github.com:mqyqingfeng/blog-course.git
git branch -M main
git push -u origin main
```

按照这些命令将本地代码提交到 GitHub 上。

然后在 Vercel 的[管理后台](https://vercel.com/dashboard)，新建项目，导入该仓库（需要先授权 GitHub）：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/74e857e08ff44e269ddff4620d6e5c37~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1640\&h=498\&s=62748\&e=png\&b=0b0b0b)

导入后，点击 Deploy 按钮进行构建和部署：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/3250c61f8c304977a758014cc4b2361a~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2790\&h=1646\&s=235659\&e=png\&b=080808)

大概 1 分钟左右部署完成，进入部署页面：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/2a2a48a2eb7d4bb28f56daa0dbe58cb2~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=3562\&h=1462\&s=312255\&e=png\&b=050505)

查看页面线上效果：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/ac83cc5064224deaaaf2cc4ae1da74b0~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=3074\&h=1194\&s=216616\&e=png\&b=ffffff)

如果在构建部署的出现这类错误：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/2af1f1afb6e14a61a024722727ced5d5~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2910\&h=852\&s=240342\&e=png\&b=000000)

参考 [Supported Node.js versions](https://vercel.com/docs/functions/runtimes/node-js/node-js-versions)，在 `package.json` 中添加如下代码：

```javascript
"engines": {
  "node": "20.x"
}
```

如果访问具体文章页时出现了这种错误：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/1cbd8a6c65fb4b5ba67ac85b45314701~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1350\&h=590\&s=59327\&e=png\&b=fefefe)

此时查看 Vercel 后台的日志：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/df9eb42fea464bc391765f47d8c2bf39~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=4422\&h=884\&s=439215\&e=png\&b=090909)

这个问题怀疑是 Vercel 的 bug，其实只要清理页面的缓存就会恢复：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/0216c5a6e38c4ca88eee815f892969c3~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1800\&h=1100\&s=219573\&e=png\&b=2a2a2a)

### 5.3. 自定义域名

如果我们想要使用自定义的域名呢？首先你要有一个自己的域名，域名购买和备案可以参考[《一篇域名从购买到备案到解析的详细教程》](https://juejin.cn/post/7052257775270756366)。

假设你已经有了域名，比如我有了 `yayujs.com` 这个域名，我希望将 `nextdemo.yayujs.com`这个域名解析到 Vercel 部署的地址。打开[域名控制台](https://dc.console.aliyun.com/next/index?#/domain-list/all)（我是在万网买的域名，所以这里链接的地址是阿里云域名控制台），选择对应的域名，点击“解析”。

根据 [Vercel 官方文档域名添加](https://vercel.com/docs/getting-started-with-vercel/use-existing)的介绍：

> If the domain is in use by another Vercel account, you will need to verify access to the domain, with a TXT record
>
> If you're using an Apex domain (e.g. example.com), you will need to configure it with an A record
>
> If you're using a Subdomain (e.g. docs.example.com), you will need to configure it with a CNAME record

也就是说，如果是根域，就配置 A 记录，如果是子域，就配置 CNAME 记录。这里因为配置的是子域，所以选择的 `CNAME`，配置如下：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/84eab902867f477f9aafbf3b55782870~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1544\&h=1310\&s=125775\&e=png\&b=fefefe)

其中记录值填写我们项目部署的域名。

最后在 Vercel 的设置中添加设置的域名：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/dff7d0d5740643eb915f9b43f85dfbf6~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2648\&h=1604\&s=277703\&e=png\&b=fcfcfc)

### 5.4. 国内访问

Vercel 直接部署的域名是无法访问的，我们可以通过 <https://tool.chinaz.com/dnsce> 检测我们的域名：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/29f402cd415a4ce988099dc142946643~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2382\&h=1412\&s=361124\&e=png\&b=ffffff)

如何让国内用户也可以访问呢？可以修改我们的域名解析：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/43a1c30f9b544d07a3f0c9cd8ca3f76d~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1478\&h=912\&s=103800\&e=png\&b=fdfdfd)

如果用的 A 记录，记录值为 `76.223.126.88`

如果用的 CNAME，记录值为 `cname-china.vercel-dns.com`

过一小段时间后，再次检测：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/fec97df6a411456eb2153c19d70c52f9~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2388\&h=1414\&s=363559\&e=png\&b=ffffff)

此时国内也可以正常访问了。

## 6. 最后

本篇我们实现了 SEO、RSS 订阅、预加载和 Vercel 部署。本篇涉及的拓展阅读内容有：

1.  [《理论篇 | SEO 要做哪些事情？》](https://juejin.cn/book/7452253924608147467/section/7453022682326499347)
2.  [《理论篇 | RSS 订阅》](https://juejin.cn/book/7452253924608147467/section/7453022514253021196)
3.  [《理论篇 | 性能优化要做哪些事情？》](https://juejin.cn/book/7452253924608147467/section/7453022722403074087)

读者可选择感兴趣的内容进行阅读，理论篇会有更详细完整的介绍，帮助你加深对代码的理解。当然也可以直接进入下个实战篇。

至此我们已经介绍完了实现一个静态博客涉及的知识点。

从早期的图标使用、响应式布局、暗黑模式支持、Tailwind CSS 使用到中期的 Markdown 渲染、Markdown 插件 remark、rehype 插件开发、MDX 自定义组件，再到错误页面的处理、SEO、RSS 订阅、性能优化的处理。

这些内容看似与 Astro 本身关系不大，但却是使用 Astro 实实在在会遇到的问题。其实与其说是使用 Astro 时遇到的问题，不如说这本小册是以 Astro 为例，讲解内容型网站常遇到的一些问题。在这本小册讲到的理论甚至代码，其实也可以用于其他框架中，毕竟原理是相通的。
