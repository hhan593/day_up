## 1. 前言

本篇我们盘点下 Astro 中动态导入文件相关的知识，这些知识在官方文档中也有讲到，只是被零散地分到了多个章节，本篇属于横向的梳理汇总，有动态导入文件的需求时再来查阅这篇文章。

## 2. 动态导入 API

### 2.1. Astro.glob()

Astro 提供了 [Astro.glob()](https://docs.astro.build/zh-cn/reference/api-reference/#astroglob) 用于一次导入多个文件。

该方法只有一个参数，那就是你要导入的本地文件的 Glob Patterns 地址。该方法是异步的，会返回一个包含匹配文件导出的数组。举些例子：

使用 `Astro.glob()` 导入 Markdown 文件：

```jsx
// src/components/my-component.astro --- // 导入`./src/pages/post/` 中所有以 `.md` 结尾的文件 const posts = await Astro.glob('../pages/post/*.md'); ---
<!-- 把前 5 篇博客文章渲染成 <article> -->
<div>
  {posts.slice(0, 4).map((post) => (
  <article>
    <h2>{post.frontmatter.title}</h2>
    <p>{post.frontmatter.description}</p>
    <a href="{post.url}">Read more</a>
  </article>
  ))}
</div>
```

导入的 Markdown 文件会返回 MarkdownInstance 类型的对象：

```javascript
export interface MarkdownInstance<T extends Record<string, any>> {
  /* 在此文件的 YAML frontmatter 中指定的任何数据 */
 frontmatter: T;
  /* 该文件的文件绝对路径 */
 file: string;
  /* 该文件的渲染路径 */
 url: string | undefined;
  /* 渲染此文件内容的 Astro 组件 */
 Content: AstroComponentFactory;
 /** (仅限 Markdown) Markdown 文件的原始内容，不包括布局 HTML 和 YAML frontmatter */
 rawContent(): string;
 /* (仅限 Markdown) Markdown 文件编译后的 HTML，不包括布局 HTML */
 compiledContent(): string;
 /* 返回该文件中 h1...h6 元素数组的函数 */
 getHeadings(): Promise<{ depth: number; slug: string; text: string }[]>;
 default: AstroComponentFactory;
}
```

使用 `Astro.glob()` 导入 Astro 组件：

```jsx
// src/pages/component-library.astro --- // 导入`./src/components/` 中所有以 `.astro` 结尾的文件 const components = await Astro.glob('../components/*.astro'); ---
<!-- 展示所有组件-->
{components.map((component) => (
<div>
  <component.default size="{24}" />
</div>
))}
```

导入的 Astro 组件会返回 AstroInstance 类型的对象：

```javascript
export interface AstroInstance {
  /* 此文件的文件路径 */
  file: string;
  /* 此文件的 URL（如果它在 pages 目录中）*/
  url: string | undefined;
  default: AstroComponentFactory;
}
```

可以看到导入不同的文件会返回不同结构的对象。如果你不知道文件类型包含什么，那么` Astro.glob()` 可以接受 TypeScript 泛型：

```jsx
---
interface CustomDataFile {
  default: Record<string, any>;
}
const data = await Astro.glob<CustomDataFile>('../data/**/*.js');
---
```

#### 2.1.1. Glob Patterns

所谓 Glob Patterns 是指由普通字符和 `/` 或通配符组成的字符串，用于匹配文件路径。

简单说下它的语法：

*   `星号 (* )` — 匹配除斜杠（路径分隔符）、隐藏文件（以 . 开头的名称）之外的所有内容。
*   `双星 (** )` — 匹配零个或多个目录。
*   `问号 (? )` – 匹配除斜杠（路径分隔符）之外的任何单个字符。
*   `序列 ([seq] )` — 匹配序列中的任何字符。

举些例子：

*   `./pages/**/*.{md,mdx}`– 匹配 `./pages`目录下的所有子目录（包括深层次的目录）中的 `.md` 或 `.mdx` 文件
*   `./src/*.??` — 匹配 src 目录中具有两个字符扩展名的所有文件。
*   `./src/file-[01].js` — 匹配文件： `file-0.js` 、 `file-1.js` 。

当然也有一些高级的语法，比如匹配数字、正则表达式组等语法。[点击查看 glob 语法说明文档](https://github.com/mrmlnc/fast-glob?tab=readme-ov-file#pattern-syntax)。

#### 2.1.2. 地址不支持动态变量

`Astro.glob()` 不支持动态变量和字符串插值。

这是受 Vite 的 `import.meta.glob()` 函数的限制，它只支持静态字符串字面量。

通常可以使用 `Astro.glob()` 导入包括所有你需要的文件集，然后对其进行过滤，以此来解决这个问题：

```jsx
// src/components/featured.astro --- const { postSlug } = Astro.props const pathToMyFeaturedPost = `src/pages/blog/${postSlug}.md` const posts = await Astro.glob('../pages/blog/*.md'); const
myFeaturedPost = posts.find(post => post.file.includes(pathToMyFeaturedPost)); ---

<p>Take a look at my favorite post, <a href="{myFeaturedPost.url}">{myFeaturedPost.frontmatter.title}</a>!</p>
```

### 2.2. import.meta.glob()

Astro.glob() 本质是 Vite 的 [import.meta.glob](https://cn.vite.dev/guide/features#glob-import) 的包装器。在 Astro 中，你也可以直接使用 [import.meta.glob](https://cn.vite.dev/guide/features#glob-import) 导入文件：

```javascript
const modules = import.meta.glob("./dir/*.js");
```

Vite 会转义为：

```javascript
// vite 生成的代码
const modules = {
  "./dir/foo.js": () => import("./dir/foo.js"),
  "./dir/bar.js": () => import("./dir/bar.js"),
};
```

所以如果要用模块，还需要遍历：

```javascript
for (const path in modules) {
  modules[path]().then((mod) => {
    console.log(path, mod);
  });
}
```

`import.meta.glob()` 的第一个参数支持数组形式：

```javascript
const modules = import.meta.glob(["./dir/*.js", "./another/*.js"]);
```

也支持反面 glob 匹配模式（以 ! 作为前缀）。若要忽略结果中的一些文件，你可以添加“排除匹配模式”作为第一个参数：

```javascript
const modules = import.meta.glob(["./dir/*.js", "!**/bar.js"]);

// vite 生成的代码
const modules = {
  "./dir/foo.js": () => import("./dir/foo.js"),
};
```

更多使用指南查看 [Vite 关于 import.meta.glob 的说明](https://cn.vite.dev/guide/features#glob-import)。

### 2.3. Astro.globa() vs import.meta.glob()

至于是使用 `Astro.glob()` 还是 `import.meta.glob()`：

1.  `Astro.glob()` 只在 `.astro` 文件中可用，而 `import.meta.glob()` 在项目的任何地方都可用
2.  `import.meta.glob()` 支持传入 glob 字符串数组，也支持添加反面 glob 匹配模式，而 `Astro.glob()`只能传入单个字符串
3.  `import.meta.glob()` 返回导入文件内容的函数，而不是返回内容本身，所以如果你不希望立即加载文件，使用 `import.meta.glob()`
4.  i`mport.meta.glob()` 返回文件路径到其内容的映射，而 `Astro.glob()` 返回内容的列表，所以如果需要每个文件的路径，使用 `import.meta.glob()`

## 3. 动态导入 Markdown 或 MDX 文件

### 3.1. import、import.meta.glob()、Astro.global()

#### 3.1.1. 导入 Markdown

当使用 import 或 `import.meta.glob()`（包括 Astro.global）导入 Markdown 时，以下导出的属性在你的 `.astro` 组件中可用：

*   **`file`** - 绝对文件路径（例如 `/home/user/projects/.../file.md`）。
*   **`url`** - 如果是页面，则为页面的 URL（例如 `/zh-cn/guides/markdown-content`）。
*   **`frontmatter`** - 包含了文件的 YAML frontmatter 中所指定的任何数据。
*   **`<Content />`** - 返回文件完整渲染内容的组件。
*   **`rawContent()`** - 一个函数，将原始 Markdown 文档作为字符串返回。
*   **`compiledContent()`** - 一个函数，将 Markdown 文档编译为 HTML 字符串后返回。
*   **`getHeadings()`** - 一个异步函数，用于返回文件中所有标题（`<h1>` 到 `<h6>`）的数组，类型为：`{ depth: number; slug: string; text: string }[]`。每个标题的 `slug` 都对应了给定标题生成的 ID，可用于锚点链接。



示例结构如下：



```javascript
Astro.props = {
  file: "/home/user/projects/.../file.md",
  url: "/en/guides/markdown-content/",
  frontmatter: {
    /** 从博客文章中获取的 Frontmatter */
    title: "Astro 0.18 Release",
    date: "Tuesday, July 27 2021",
    author: "Matthew Phillips",
    description: "Astro 0.18 is our biggest release since Astro launch.",
  },
  getHeadings: () => [
    { depth: 1, text: "Astro 0.18 Release", slug: "astro-018-release" },
    { depth: 2, text: "Responsive partial hydration", slug: "responsive-partial-hydration" },
    /* ... */
  ],
  rawContent: () => "# Astro 0.18 Release\nA little over a month ago, the first public beta [...]",
  compiledContent: () => "<h1>Astro 0.18 Release</h1>\n<p>A little over a month ago, the first public beta [...]</p>",
};
```



上节讲到该对象对应的类型是 MarkdownInstance：



```javascript
export interface MarkdownInstance<T extends Record<string, any>> {
  /* 在此文件的 YAML frontmatter 中指定的任何数据 */
 frontmatter: T;
  /* 该文件的文件绝对路径 */
 file: string;
  /* 该文件的渲染路径 */
 url: string | undefined;
  /* 渲染此文件内容的 Astro 组件 */
 Content: AstroComponentFactory;
 /** (仅限 Markdown) Markdown 文件的原始内容，不包括布局 HTML 和 YAML frontmatter */
 rawContent(): string;
 /* (仅限 Markdown) Markdown 文件编译后的 HTML，不包括布局 HTML */
 compiledContent(): string;
 /* 返回该文件中 h1...h6 元素数组的函数 */
 getHeadings(): Promise<{ depth: number; slug: string; text: string }[]>;
 default: AstroComponentFactory;
}
```

你可以选择使用 TypeScript 泛型指定 frontmatter 变量类型：

```jsx
---
interface Frontmatter {
  title: string;
  description?: string;
}
const posts = await Astro.glob<Frontmatter>('../pages/post/*.md');
---

<ul>
  {posts.map(post => <li>{post.frontmatter.title}</li>)}
</ul>
```

#### 3.1.2. 导入 MDX

当使用 `import` 或 `import.meta.glob()` 导入 MDX 时，以下导出的属性在你的 `.astro` 组件中可用：

*   `file` - 文件的绝对路径（例如：/home/user/projects/.../file.mdx）。
*   `url` - 页面的 URL（例如：/zh-cn/guides/markdown-content）。
*   `frontmatter` - 包含了文件的 YAML frontmatter 中所指定的任何数据。
*   `getHeadings()` - 一个异步函数，用于返回文件中所有标题（`<h1>` 到 `<h6>`）的数组，类型为：{ depth: number; slug: string; text: string }\[]。每个标题的 slug 都对应了给定标题生成的 ID，可用于锚点链接。
*   `<Content />` - 返回文件完整渲染内容的组件。
*   （任意的 export 值） - MDX 文件还可以使用 export 语句以导出数据。

示例对象如下：

```json
{
  file: "/home/user/projects/.../file.mdx",
  url: "/en/guides/markdown-content/",
  frontmatter: {
    /** 从博客文章中获取的 Frontmatter */
    title: "Astro 0.18 Release",
    date: "Tuesday, July 27 2021",
    author: "Matthew Phillips",
    description: "Astro 0.18 is our biggest release since Astro launch.",
  },
  getHeadings: () => [
    {"depth": 1, "text": "Astro 0.18 Release", "slug": "astro-018-release"},
    {"depth": 2, "text": "Responsive partial hydration", "slug": "responsive-partial-hydration"}
    /* ... */
  ]
}
```

MDX 相比于 Markdown 少了 rawContent 和 compiledContent，但多了 `export const` 导出的属性。

### 3.2. 内容集合

**如果是导入 Markdown 或者 MDX，还是应该优先使用 Astro 内置的内容集合功能。**

***

所谓[内容集合](https://docs.astro.build/zh-cn/guides/content-collections/)，初次接触的时候可能有些迷惑，其实就是 Astro 和你约定，你将 MD 或者 MDX 文档放入约定的目录中，然后 Astro 提供更加便捷的 API 让你管理和操作这些文件。

我们来简单看下流程。

首先你要将 MD 或 MDX 文档放入到 `src/content`中的直接子目录，比如 `src/content/posts`、`src/content/authors` 下。如果放到 `src/content/posts`下，对应的内容集合名字就是 `posts`，其他同理。

Astro 提供了 getCollection API 用于获取内容集合。此时你就可以直接获取该集合的数据：

```javascript
import type { CollectionEntry } from "astro:content";
import { getCollection } from "astro:content";

const posts: CollectionEntry<"posts">[] = await getCollection("posts");
```

但 Astro 会建议你先写个 Schema，定义每个 Markdown 文件的 frontmatter，以保证所需数据不会漏掉，这就是 `src/content/config.ts`这个文件的作用：

```javascript
import { z, defineCollection } from "astro:content";

const postsCollection = defineCollection({
  type: "content",
  schema: z.object({
    title: z.string(),
    columns: z.array(z.string()).default(["uncategory"]),
    published: z.string().refine((arg) => arg.match(/^(\d{4})-(\d{2})-(\d{2}) (\d{2}):(\d{2}):(\d{2}(?:\.\d*)?)$/)),
  }),
});

export const collections = {
  posts: postsCollection,
};
```

在这段代码中，我们定义了 posts 集合对应的 Schema，其中，每个 MD 或 MDX 文档都必须有 `title`、`colums`、`published` 字段，这些字段的格式也有定义。如果 Astro 检测到文档的数据不符合定义的格式，就会报错提示，这就避免了因为忘记定义数据或者定义格式错误而导致的渲染错误。

`getCollection()` 也支持筛选数据：

```javascript
import { getCollection } from "astro:content";
const draftBlogEntries = await getCollection("blog", ({ data }) => {
  return data.draft !== true;
});
```

除了 `getCollection()`，Astro 还提供了`getEntries()`用于获取单个集合条目：

```javascript
import { getEntry } from "astro:content";

// 得到 `src/content/blog/enterprise.md`
const enterprisePost = await getEntry("blog", "enterprise");
```

内容集合的基本内容就这些，当导入 MD 或 MDX 文档时，优先使用内容集合，实战篇采用的正是内容集合的方式。

当使用 getCollection 的时候，返回的是 [CollectionEntry](https://docs.astro.build/zh-cn/reference/modules/astro-content/#collectionentry) 类型的对象数组：

```javascript
import type { CollectionEntry } from "astro:content";
```

对象的结构如下：

*   `id` - 文件 id，你可以简单理解为文件名
*   `collection` - 集合名
*   `data` - 一个从集合模式推断出的 frontmatter 属性对象
*   `slug` - 可用于 Markdown 或 MDX 文档的 URL 标头。默认为不含文件扩展名的 “id”，但可以通过在文件的 frontmatter 中设置 slug 属性来覆盖
*   `body` - 一个包含 Markdown 或 MDX 文档原始未编译的 body 的字符串
*   `render()` - 一个用于编译给定的 Markdown 或 MDX 文档以进行渲染的函数，它会返回 3 个字段：
    *   `<Content />` - 用于在 Astro 文件中渲染文档内容的组件
    *   `headings` - 生成的标题列表，与通过导入 Markdown 和 MDX 返回的 getHeadings() 工具函数相同
    *   `remarkPluginFrontmatter` - 在应用 remark 或 rehype 插件后修改后的 frontmatter 对象

## 4. 动态导入图片

在 Astro 项目开发中，常会遇到动态导入图片的问题，比如在我的博客有一个 [Sponsor](https://yayujs.com/sponsors) 页面，用于感谢打赏的同学：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/f121a5692c344b51b56d800eff9acd0a~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1972\&h=820\&s=169444\&e=png\&b=fefefe)

虽然可以复制代码一个一个导入，但使用动态导入，只需要修改一下配置即可实现。

使用 Astro.glob() 或 import.meta.glob() 都是可以的，写法会略有不同：

使用 `import.meta.glob()`：

```jsx
---
import { Image } from "astro:assets";
const imagesList = import.meta.glob<{ default: ImageMetadata }>(
  "/src/images/sponsors/*.{jpeg,jpg,JPG,png,PNG,gif}",
);
---

[...].map((path) => {
  return (
    <Image
      src={path ? imagesList[path]() : "https://xsgames.co/randomusers/avatar.php?g=pixel"}
      width="256"
      height="256"
      alt="sponsor avators"
      class="rounded-full mb-2 sponsors"
    />
  )
})
```

使用 `Astro.glob()`：

```jsx
---
import { Image } from "astro:assets";
const imagesList = await Astro.glob<{ default: ImageMetadata }>(
  "/src/images/sponsors/*.{jpeg,jpg,JPG,png,PNG,gif}",
);
---

[...].map((path) => {
  return (
    <Image
      src={img.default}
      width="256"
      height="256"
      alt="sponsor avators"
      class="rounded-full mb-2 sponsors"
    />
  )
})
```