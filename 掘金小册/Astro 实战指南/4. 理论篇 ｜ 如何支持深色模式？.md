## 1. 前言

本篇我们来实现深色模式。它也被叫做暗黑模式、黑夜模式等等。这里我们取的是苹果官方文档的翻译：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/68cbcebd0c4e43b68e5d92fa222dabb0~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=2104\&h=616\&s=266452\&e=png\&b=fdfdfd)

## 2. 深色模式

“深色模式”本质上是一套采用深色背景、浅色文案的配色方案。其实很早就有。现在越来越流行，是因为苹果在 2018 年提出了这一概念。

先是 2018 年的 macOS Mojave 率先支持了深色外观，然后 2019 年 IOS 13 正式支持深色外观。这些都是系统级别的支持，可以将整个系统的界面切换为深色模式。由此深色模式开始进入人们的视野，成为设计师、前端工程师的学习内容……

使用深色模式也确实有一些好处：

1.  **保护视力。** 毕竟晚上的时候，浅色有些刺眼。
2.  **增加沉浸感。** 阅读浏览类 App 或是内容创作型 App（比如代码编辑器）往往会使用深色模式，它们会借助黑底白字的高对比度特性让用户视线保持集中。所以切换深色模式后，可以增加沉浸感，提升使用欲望。
3.  **省电。** 根据谷歌的官方数据，采用 OLED 屏幕的手机在「深色模式」下，耗电可下降达 63%。

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/71ac532f6ef64f8eb8e232db3a3624f2~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=3024\&h=1516\&s=396568\&e=png\&b=111111)

## 3. 关于适配深色模式的基础知识

如果我们开发页面，该如何支持深色模式呢？**我们先讲支持深色模式的原理，最后再讲在 Astro 中如何实现。**

### 3.1. CSS 媒体查询

**CSS 提供了 **[**prefers-color-scheme**](https://developer.mozilla.org/zh-CN/docs/Web/CSS/@media/prefers-color-scheme)** 媒体查询特性，用于检测用户是否有将系统的主题色设置为浅色或者深色。**

比如 Next.js 脚手架默认创建项目的 `app/globals.css`中，就有这样一段代码：

```css
:root {
  --foreground-rgb: 0, 0, 0;
  --background-start-rgb: 214, 219, 220;
  --background-end-rgb: 255, 255, 255;
}

@media (prefers-color-scheme: dark) {
  :root {
    --foreground-rgb: 255, 255, 255;
    --background-start-rgb: 0, 0, 0;
    --background-end-rgb: 0, 0, 0;
  }
}
```

这就是一段根据系统的主题色调整网页基础样式的代码。此时页面会根据系统的外观模式进行调整，效果如下：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/3aa9292308a647e09c1a139c2f6b566a~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=923\&h=630\&s=471100\&e=gif\&f=40\&b=ececee)

注：浏览器设置里也有设置模式的地方：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/68a039c3920d4a41a53c6b691a4be1be~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1848\&h=444\&s=81383\&e=png\&b=26272a)

但这里设置的是浏览器的外观，不会影响具体页面的外观模式。

### 3.2. JS 查询

CSS 查询会自动跟随系统设置，但如果你要自定义外观模式，就比如很多博客页面右上角都有个外观模式按钮，点击可以切换到 light / dark 模式，这种就需要依赖 JS 了。

Web API 提供了 [Window.matchMedia()](https://developer.mozilla.org/zh-CN/docs/Web/API/Window/matchMedia) 方法，它会返回一个新的 MediaQueryList 对象，表示指定的媒体查询字符串解析后的结果。返回的 MediaQueryList 可被用于判定 Document 是否匹配媒体查询，或者监控一个 document 来判定它匹配了或者停止匹配了此媒体查询。

#### 3.2.1. 判断浏览器是否支持深色模式

```javascript
if (window.matchMedia("(prefers-color-scheme)").media !== "not all") {
  console.log("🎉 Dark mode is supported");
}
```

其实支持率还蛮好的：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/89cf02c1bb3d48d6a00c37143bf6fa54~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=3430\&h=1442\&s=524605\&e=png\&b=f0e8d8)

#### 3.2.2. 监听深色模式变化

如果我们要监听深色模式变化，可以这样写：

```javascript
const darkModeMediaQuery = window.matchMedia("(prefers-color-scheme: dark)");

darkModeMediaQuery.addEventListener("change", (e) => {
  const darkModeOn = e.matches;
  console.log(`Dark mode is ${darkModeOn ? "🌒 on" : "☀️ off"}.`);
});
```

### 3.3. 测试不同的模式

如果要测试页面的浅色/深色模式，修改系统的外观模式会有些麻烦，Chrome 提供了快速切换的方式：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/b37ee4faa0e9436985208d270a18035a~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1196\&h=558\&s=366613\&e=gif\&f=32\&b=fcfcfc)

其步骤为：

1.  打开浏览器开发者工具
2.  Command + Shift + P 打开命令
3.  输入 dark 或者 light 搜索命令
4.  回车确定

注：这只是用于测试，效果是暂时的，关闭开发者工具，就会退出设置的样式。

## 4. Astro 实现深色模式

基础知识我们就说这么多，只是帮助大家理解深色模式的概念和实现的基本原理。现在我们来讲讲在 Astro 中如何实现深色模式。

### 4.1. 无客户端导航

其实在 Astro 官方的 [Turitoal](https://docs.astro.build/zh-cn/tutorial/6-islands/2/) 中就有关于深色模式的实现，我们仿照实现一下。

新建 `src/components/ThemeIcon.astro`，代码如下：

```jsx
---
---

<button id="themeToggle">
  <svg width="30px" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
    <path
      class="sun"
      fill-rule="evenodd"
      d="M12 17.5a5.5 5.5 0 1 0 0-11 5.5 5.5 0 0 0 0 11zm0 1.5a7 7 0 1 0 0-14 7 7 0 0 0 0 14zm12-7a.8.8 0 0 1-.8.8h-2.4a.8.8 0 0 1 0-1.6h2.4a.8.8 0 0 1 .8.8zM4 12a.8.8 0 0 1-.8.8H.8a.8.8 0 0 1 0-1.6h2.5a.8.8 0 0 1 .8.8zm16.5-8.5a.8.8 0 0 1 0 1l-1.8 1.8a.8.8 0 0 1-1-1l1.7-1.8a.8.8 0 0 1 1 0zM6.3 17.7a.8.8 0 0 1 0 1l-1.7 1.8a.8.8 0 1 1-1-1l1.7-1.8a.8.8 0 0 1 1 0zM12 0a.8.8 0 0 1 .8.8v2.5a.8.8 0 0 1-1.6 0V.8A.8.8 0 0 1 12 0zm0 20a.8.8 0 0 1 .8.8v2.4a.8.8 0 0 1-1.6 0v-2.4a.8.8 0 0 1 .8-.8zM3.5 3.5a.8.8 0 0 1 1 0l1.8 1.8a.8.8 0 1 1-1 1L3.5 4.6a.8.8 0 0 1 0-1zm14.2 14.2a.8.8 0 0 1 1 0l1.8 1.7a.8.8 0 0 1-1 1l-1.8-1.7a.8.8 0 0 1 0-1z"
    ></path>
    <path
      class="moon"
      fill-rule="evenodd"
      d="M16.5 6A10.5 10.5 0 0 1 4.7 16.4 8.5 8.5 0 1 0 16.4 4.7l.1 1.3zm-1.7-2a9 9 0 0 1 .2 2 9 9 0 0 1-11 8.8 9.4 9.4 0 0 1-.8-.3c-.4 0-.8.3-.7.7a10 10 0 0 0 .3.8 10 10 0 0 0 9.2 6 10 10 0 0 0 4-19.2 9.7 9.7 0 0 0-.9-.3c-.3-.1-.7.3-.6.7a9 9 0 0 1 .3.8z"
    ></path>
  </svg>
</button>

<style>
  #themeToggle {
    border: 0;
    background: none;
  }
  .sun {
    fill: black;
  }
  .moon {
    fill: transparent;
  }

  :global(.dark) .sun {
    fill: transparent;
  }
  :global(.dark) .moon {
    fill: white;
  }
</style>

<script is:inline>
  const theme = (() => {
    if (typeof localStorage !== "undefined" && localStorage.getItem("theme")) {
      return localStorage.getItem("theme");
    }
    if (window.matchMedia("(prefers-color-scheme: dark)").matches) {
      return "dark";
    }
    return "light";
  })();

  if (theme === "light") {
    document.documentElement.classList.remove("dark");
  } else {
    document.documentElement.classList.add("dark");
  }

  window.localStorage.setItem("theme", theme);

  const handleToggleClick = () => {
    const element = document.documentElement;
    element.classList.toggle("dark");

    const isDark = element.classList.contains("dark");
    localStorage.setItem("theme", isDark ? "dark" : "light");
  };

  document.getElementById("themeToggle").addEventListener("click", handleToggleClick);
</script>
```

在这段代码中，我们会首先从 `localStorage`中判断是否有之前设置的主题值，如果没有，就判断当前是否是深色模式，然后返回当前的主题值。然后我们会将该主题值放到 html 的 class 上，这样我们就可以通过 `.dark xxx`声明深色模式样式或者通过 Tailwind CSS 的 `dark:xxx`声明。然后我们会将值存到 localStorage，方便下次判断。

当用户点击主题按钮的时候，我们会切换 html 的 `dark`类名，这样就实现了浅色和深色的切换。

我们的按钮是一个 SVG 图标，它由一个 Sun 图标和一个 Moon 图标组成。在样式设置的时候，一个透明（`fill: transparent`），一个不透明 （`fill: black`），根据 `dark`类名切换样式，这样我们就实现了点击时的图标切换效果。

新建 `src/layouts/Layout.astro`，代码如下：

```jsx
---
import ThemeIcon from "../components/ThemeIcon.astro";
---

<!DOCTYPE html>
<html lang="zh">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content="{Astro.generator}" />
    <title>Dark Mode Test</title>
  </head>
  <body>
    <header>
      <ThemeIcon />
    </header>
    <slot />
  </body>
</html>

<style>
  html.dark {
    background-color: #0d0950;
    color: #fff;
  }

  .dark .nav-links a {
    color: #fff;
  }

  body {
    padding: 20px;
  }
</style>
```

新建 `src/pages/theme1.astro`，代码如下：

```jsx
---
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <a href="/theme1">theme1</a>
  <a href="/theme2">theme2</a>
</Layout>
```

新建 `src/pages/theme2.astro`，代码如下：

```jsx
---
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <a href="/theme1">theme1</a>
  <a href="/theme2">theme2</a>
</Layout>
```

浏览器效果如下：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/41e3af53226e4aefad81f17a79c564f2~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1023\&h=522\&s=1147478\&e=gif\&f=71\&b=2c2c2c)

效果描述：点击图标，可以实现浅色和深色模式的切换，而且跳转到其他页面，模式也能继续保持。

### 4.2. 有客户端导航

这样看似已经没有问题了，可是如果 Astro 开启了客户端导航，模式切换可能就有问题了：

我们修改下 `src/layouts/Layout.astro`，代码如下：

```diff
---
import ThemeIcon from "../components/ThemeIcon.astro";
+import { ViewTransitions } from "astro:transitions";
---

<!doctype html>
<html lang="zh">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>Dark Mode Test</title>
+   <ViewTransitions />
  </head>
  <body>
    <header>
      <ThemeIcon />
    </header>
    <slot />
  </body>
</html>

<style>
  html.dark {
    background-color: #0d0950;
    color: #fff;
  }

  .dark .nav-links a {
    color: #fff;
  }

  body {
    padding: 20px;
  }
</style>
```

浏览器效果如下：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/0a9a7e7517b940e59a6d2c17a6964329~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1023\&h=522\&s=738610\&e=gif\&f=64\&b=2c2c2c)

效果描述：首次加载的时候可以正确切换模式，但是页面切换后（此时因为开启了客户端导航，页面并无浏览器刷新行为），不仅模式没有保持，而且无法再切换模式，直到刷新页面后才可以继续切换。

那么我们该如何解决这个问题呢？

这是因为当使用 ViewTransition 的时候，某些代码只在会页面首次加载的时候执行一次，在导航到其他页面的时候就不再执行了。我们设置主题的代码就是只执行了一次，而在页面导航的时候没有执行，所以才出现错乱。

为此我们需要借助 [astro:after-swap](https://docs.astro.build/zh-cn/guides/view-transitions/#astroafter-swap) 这个 API，根据介绍：

> 在新页面替换旧页面后立即触发的事件。你可以在 `document` 上监听此事件，并触发在新页面的 DOM 元素渲染和脚本运行之前发生的操作。
>
> 此事件在**旧页面**上监听时，可用于传递和恢复需要转移到新页面的 DOM 上的任何状态。
>
>
> 这是生命周期中的最后一个点，仍然可以安全地在其中添加一个深色模式类名（`<html class="dark-mode">`），尽管你可能希望在更早的事件中这样做。

修改 `src/components/ThemeIcon.astro`，完整代码如下：

```jsx
---
---

<button id="themeToggle">
  <svg width="30px" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
    <path
      class="sun"
      fill-rule="evenodd"
      d="M12 17.5a5.5 5.5 0 1 0 0-11 5.5 5.5 0 0 0 0 11zm0 1.5a7 7 0 1 0 0-14 7 7 0 0 0 0 14zm12-7a.8.8 0 0 1-.8.8h-2.4a.8.8 0 0 1 0-1.6h2.4a.8.8 0 0 1 .8.8zM4 12a.8.8 0 0 1-.8.8H.8a.8.8 0 0 1 0-1.6h2.5a.8.8 0 0 1 .8.8zm16.5-8.5a.8.8 0 0 1 0 1l-1.8 1.8a.8.8 0 0 1-1-1l1.7-1.8a.8.8 0 0 1 1 0zM6.3 17.7a.8.8 0 0 1 0 1l-1.7 1.8a.8.8 0 1 1-1-1l1.7-1.8a.8.8 0 0 1 1 0zM12 0a.8.8 0 0 1 .8.8v2.5a.8.8 0 0 1-1.6 0V.8A.8.8 0 0 1 12 0zm0 20a.8.8 0 0 1 .8.8v2.4a.8.8 0 0 1-1.6 0v-2.4a.8.8 0 0 1 .8-.8zM3.5 3.5a.8.8 0 0 1 1 0l1.8 1.8a.8.8 0 1 1-1 1L3.5 4.6a.8.8 0 0 1 0-1zm14.2 14.2a.8.8 0 0 1 1 0l1.8 1.7a.8.8 0 0 1-1 1l-1.8-1.7a.8.8 0 0 1 0-1z"
    ></path>
    <path
      class="moon"
      fill-rule="evenodd"
      d="M16.5 6A10.5 10.5 0 0 1 4.7 16.4 8.5 8.5 0 1 0 16.4 4.7l.1 1.3zm-1.7-2a9 9 0 0 1 .2 2 9 9 0 0 1-11 8.8 9.4 9.4 0 0 1-.8-.3c-.4 0-.8.3-.7.7a10 10 0 0 0 .3.8 10 10 0 0 0 9.2 6 10 10 0 0 0 4-19.2 9.7 9.7 0 0 0-.9-.3c-.3-.1-.7.3-.6.7a9 9 0 0 1 .3.8z"
    ></path>
  </svg>
</button>

<style>
  #themeToggle {
    border: 0;
    background: none;
  }
  .sun {
    fill: black;
  }
  .moon {
    fill: transparent;
  }

  :global(.dark) .sun {
    fill: transparent;
  }
  :global(.dark) .moon {
    fill: white;
  }
</style>

<script is:inline>
  const setTheme = () => {
    const theme = (() => {
      if (typeof localStorage !== "undefined" && localStorage.getItem("theme")) {
        return localStorage.getItem("theme");
      }
      if (window.matchMedia("(prefers-color-scheme: dark)").matches) {
        return "dark";
      }
      return "light";
    })();

    if (theme === "light") {
      document.documentElement.classList.remove("dark");
    } else {
      document.documentElement.classList.add("dark");
    }

    window.localStorage.setItem("theme", theme || "light");
  };

  setTheme();

  document.addEventListener("astro:after-swap", setTheme);

  const setToggleListener = () => {
    let toggleBtn = document.getElementById("themeToggle");
    const handleToggleClick = () => {
      const element = document.documentElement;
      element.classList.toggle("dark");

      const isDark = element.classList.contains("dark");
      localStorage.setItem("theme", isDark ? "dark" : "light");
    };

    toggleBtn?.addEventListener("click", handleToggleClick);
  };

  setToggleListener();

  document.addEventListener("astro:after-swap", setToggleListener);
</script>
```

此时浏览器效果恢复正常：

![](https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/ffb8e329e62b4be0abcd30a488236bce~tplv-k3u1fbpfcp-jj-mark:0:0:0:0:q75.image#?w=1023\&h=522\&s=685380\&e=gif\&f=74\&b=2c2c2c)

### 4.3. 最佳实践

查看 `ThemeIcon.astro`中的脚本代码，其实代码主要分为 2 部分：设置当前主题和为图标添加切换事件。

考虑到初始加载页面时的代码阻塞，设置当前主题这部分的代码其实最好应该放到 `Layout.astro`中的 `<head>` 中。

修改 `src/layouts/Layout.astro`，将设置主题的代码加入到 head 中，完整代码如下：

```jsx
---
import ThemeIcon from "../components/ThemeIcon.astro";
import { ViewTransitions } from "astro:transitions";
---

<!DOCTYPE html>
<html lang="zh">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content="{Astro.generator}" />
    <title>Dark Mode Test</title>
    <script is:inline>
      const setTheme = () => {
        const theme = (() => {
          if (typeof localStorage !== "undefined" && localStorage.getItem("theme")) {
            return localStorage.getItem("theme");
          }
          if (window.matchMedia("(prefers-color-scheme: dark)").matches) {
            return "dark";
          }
          return "light";
        })();

        if (theme === "light") {
          document.documentElement.classList.remove("dark");
        } else {
          document.documentElement.classList.add("dark");
        }

        window.localStorage.setItem("theme", theme || "light");
      };

      setTheme();

      document.addEventListener("astro:after-swap", setTheme);
    </script>
    <ViewTransitions />
  </head>
  <body>
    <header>
      <ThemeIcon />
    </header>
    <slot />
  </body>
</html>

<style>
  html.dark {
    background-color: #0d0950;
    color: #fff;
  }

  .dark .nav-links a {
    color: #fff;
  }

  body {
    padding: 20px;
  }
</style>
```

修改 `src/components/ThemeIcon.astro`，删除掉设置主题的代码，完整代码如下：

```jsx
---
---

<button id="themeToggle">
  <svg width="30px" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
    <path
      class="sun"
      fill-rule="evenodd"
      d="M12 17.5a5.5 5.5 0 1 0 0-11 5.5 5.5 0 0 0 0 11zm0 1.5a7 7 0 1 0 0-14 7 7 0 0 0 0 14zm12-7a.8.8 0 0 1-.8.8h-2.4a.8.8 0 0 1 0-1.6h2.4a.8.8 0 0 1 .8.8zM4 12a.8.8 0 0 1-.8.8H.8a.8.8 0 0 1 0-1.6h2.5a.8.8 0 0 1 .8.8zm16.5-8.5a.8.8 0 0 1 0 1l-1.8 1.8a.8.8 0 0 1-1-1l1.7-1.8a.8.8 0 0 1 1 0zM6.3 17.7a.8.8 0 0 1 0 1l-1.7 1.8a.8.8 0 1 1-1-1l1.7-1.8a.8.8 0 0 1 1 0zM12 0a.8.8 0 0 1 .8.8v2.5a.8.8 0 0 1-1.6 0V.8A.8.8 0 0 1 12 0zm0 20a.8.8 0 0 1 .8.8v2.4a.8.8 0 0 1-1.6 0v-2.4a.8.8 0 0 1 .8-.8zM3.5 3.5a.8.8 0 0 1 1 0l1.8 1.8a.8.8 0 1 1-1 1L3.5 4.6a.8.8 0 0 1 0-1zm14.2 14.2a.8.8 0 0 1 1 0l1.8 1.7a.8.8 0 0 1-1 1l-1.8-1.7a.8.8 0 0 1 0-1z"
    ></path>
    <path
      class="moon"
      fill-rule="evenodd"
      d="M16.5 6A10.5 10.5 0 0 1 4.7 16.4 8.5 8.5 0 1 0 16.4 4.7l.1 1.3zm-1.7-2a9 9 0 0 1 .2 2 9 9 0 0 1-11 8.8 9.4 9.4 0 0 1-.8-.3c-.4 0-.8.3-.7.7a10 10 0 0 0 .3.8 10 10 0 0 0 9.2 6 10 10 0 0 0 4-19.2 9.7 9.7 0 0 0-.9-.3c-.3-.1-.7.3-.6.7a9 9 0 0 1 .3.8z"
    ></path>
  </svg>
</button>

<style>
  #themeToggle {
    border: 0;
    background: none;
  }
  .sun {
    fill: black;
  }
  .moon {
    fill: transparent;
  }

  :global(.dark) .sun {
    fill: transparent;
  }
  :global(.dark) .moon {
    fill: white;
  }
</style>

<script is:inline>
  const setToggleListener = () => {
    let toggleBtn = document.getElementById("themeToggle");
    const handleToggleClick = () => {
      const element = document.documentElement;
      element.classList.toggle("dark");

      const isDark = element.classList.contains("dark");
      localStorage.setItem("theme", isDark ? "dark" : "light");
    };

    toggleBtn?.addEventListener("click", handleToggleClick);
  };

  setToggleListener();

  document.addEventListener("astro:after-swap", setToggleListener);
</script>
```

这样我们就完成了一个暗黑模式的支持。

### 4.4. 其他实现方式

现在我们实现的代码是通过在 html 上加一个 `dark`类名来实现的，有的时候，你可能希望改为通过在 html 上加一个` [data-theme]`属性来实现，这个时候就只用简单修改一下代码就行。修改 `src/layouts/Layout.astro`，代码如下：

```jsx
---
import ThemeIcon from "../components/ThemeIcon.astro";
import { ViewTransitions } from "astro:transitions";
---

<!DOCTYPE html>
<html lang="zh">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content="{Astro.generator}" />
    <title>Dark Mode Test</title>
    <script is:inline>
      const setTheme = () => {
        const theme = (() => {
          if (typeof localStorage !== "undefined" && localStorage.getItem("theme")) {
            return localStorage.getItem("theme");
          }
          if (window.matchMedia("(prefers-color-scheme: dark)").matches) {
            return "dark";
          }
          return "light";
        })();

        if (theme === "light") {
          document.documentElement.setAttribute("data-theme", "light");
        } else {
          document.documentElement.setAttribute("data-theme", "dark");
        }

        window.localStorage.setItem("theme", theme || "light");
      };
      setTheme();

      document.addEventListener("astro:after-swap", setTheme);
    </script>
    <ViewTransitions />
  </head>
  <body>
    <header>
      <ThemeIcon />
    </header>
    <slot />
  </body>
</html>

<style>
  html.dark {
    background-color: #0d0950;
    color: #fff;
  }

  .dark .nav-links a {
    color: #fff;
  }

  body {
    padding: 20px;
  }
</style>
```

修改 `src/components/ThemeIcon.astro`，完整代码如下：

```jsx
---
---

<button id="themeToggle">
  <svg width="30px" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
    <path
      class="sun"
      fill-rule="evenodd"
      d="M12 17.5a5.5 5.5 0 1 0 0-11 5.5 5.5 0 0 0 0 11zm0 1.5a7 7 0 1 0 0-14 7 7 0 0 0 0 14zm12-7a.8.8 0 0 1-.8.8h-2.4a.8.8 0 0 1 0-1.6h2.4a.8.8 0 0 1 .8.8zM4 12a.8.8 0 0 1-.8.8H.8a.8.8 0 0 1 0-1.6h2.5a.8.8 0 0 1 .8.8zm16.5-8.5a.8.8 0 0 1 0 1l-1.8 1.8a.8.8 0 0 1-1-1l1.7-1.8a.8.8 0 0 1 1 0zM6.3 17.7a.8.8 0 0 1 0 1l-1.7 1.8a.8.8 0 1 1-1-1l1.7-1.8a.8.8 0 0 1 1 0zM12 0a.8.8 0 0 1 .8.8v2.5a.8.8 0 0 1-1.6 0V.8A.8.8 0 0 1 12 0zm0 20a.8.8 0 0 1 .8.8v2.4a.8.8 0 0 1-1.6 0v-2.4a.8.8 0 0 1 .8-.8zM3.5 3.5a.8.8 0 0 1 1 0l1.8 1.8a.8.8 0 1 1-1 1L3.5 4.6a.8.8 0 0 1 0-1zm14.2 14.2a.8.8 0 0 1 1 0l1.8 1.7a.8.8 0 0 1-1 1l-1.8-1.7a.8.8 0 0 1 0-1z"
    ></path>
    <path
      class="moon"
      fill-rule="evenodd"
      d="M16.5 6A10.5 10.5 0 0 1 4.7 16.4 8.5 8.5 0 1 0 16.4 4.7l.1 1.3zm-1.7-2a9 9 0 0 1 .2 2 9 9 0 0 1-11 8.8 9.4 9.4 0 0 1-.8-.3c-.4 0-.8.3-.7.7a10 10 0 0 0 .3.8 10 10 0 0 0 9.2 6 10 10 0 0 0 4-19.2 9.7 9.7 0 0 0-.9-.3c-.3-.1-.7.3-.6.7a9 9 0 0 1 .3.8z"
    ></path>
  </svg>
</button>

<style>
  #themeToggle {
    border: 0;
    background: none;
  }
  .sun {
    fill: black;
  }
  .moon {
    fill: transparent;
  }

  :global([data-theme="dark"]) .sun {
    fill: transparent;
  }
  :global([data-theme="dark"]) .moon {
    fill: white;
  }
</style>

<script is:inline>
  const setToggleListener = () => {
    let toggleBtn = document.getElementById("themeToggle");
    const handleToggleClick = () => {
      const element = document.documentElement;
      const isDark = element.dataset.theme === "dark";
      element.dataset.theme = isDark ? "light" : "dark";
      localStorage.setItem("theme", isDark ? "light" : "dark");
    };

    toggleBtn?.addEventListener("click", handleToggleClick);
  };

  setToggleListener();

  document.addEventListener("astro:after-swap", setToggleListener);
</script>
```

使用 `[data-theme] = 'dark'` 和使用 `.dark`的区别在于定义暗黑模式样式的方式不同。

```css
.sidebar {
  background: var(--light-side-bg);
}

/* 使用 .dark */
.dark {
  .sidebar {
    background: var(--dark-side-bg);
  }
}

/* 使用 [data-theme="dark"] */
[data-theme="dark"] {
  .sidebar {
    background: var(--dark-side-bg);
  }
}
```

此外，Tailwind CSS 的配置略有不同：

```javascript
// 使用 [data-theme] = 'dark'
export default {
  darkMode: ["selector", '[data-theme="dark"]']
};

// .dark
export default {
  darkMode: ["selector"]
};
```
