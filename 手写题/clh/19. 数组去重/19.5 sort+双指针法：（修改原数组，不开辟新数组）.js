/**
 * @author: qiqi
 * @description: 双指针去重数组
 */

// 定义一个数组，包含一些整数
const arr = [1, 2, 3, 3, 3, 3, 3, 3, 8, 9, 10];

/**
 * 定义一个函数deboulefun，用于去除数组中的重复元素
 * @param {Array} arr - 待处理的数组，应包含可排序的元素
 * @returns {Array} - 去重后的数组
 */
const deboulefun = (arr) => {
  // 对数组进行排序，以便后续去重
  arr.sort((a, b) => a - b);

  // 初始化快慢指针，用于遍历数组
  var fast = 1,
    slow = 0;

  // 当快指针未触及数组末尾时，继续循环
  while (fast < arr.length) {
    // 如果快慢指针指向的元素相同，则说明遇到重复元素
    if (arr[slow] === arr[fast]) {
      // 移除快指针位置的重复元素
      arr.splice(fast, 1);
    } else {
      // 如果当前元素不重复，移动快慢指针到下一个元素
      fast++;
      slow++;
    }
  }

  // 返回去重后的数组
  return arr;
};

/**
 * 
 * 
 *   arr.sort((a, b) => a - b);
 * 排序逻辑：使用了一个比较函数 (a, b) => a - b 来确保数组按照升序排列。
如果 a - b < 0，则 a 会被排在 b 之前。
如果 a - b > 0，则 b 会被排在 a 之前。
如果 a - b === 0，则 a 和 b 的相对位置不变。
因此，这段代码会将数组中的元素从小到大排序。

为什么需要排序？
在这段代码中，排序是为了方便后续使用双指针法去除重复元素。通过先对数组进行排序，可以确保相同的元素会相邻排列，从而可以通过简单的比较相邻元素来检测和移除重复项。

示例
假设输入数组为 [1, 2, 3, 3, 3, 3, 3, 3, 8, 9, 10]，经过排序后仍然是 [1, 2, 3, 3, 3, 3, 3, 3, 8, 9, 10]（因为已经是有序的）。如果输入数组是无序的，例如 [3, 1, 2, 3, 8, 9, 10, 3, 3, 3, 3]，排序后的结果将是 [1, 2, 3, 3, 3, 3, 3, 3, 8, 9, 10]，这样就可以方便地去除重复的 3。

总结
排序是去重算法中的一个重要步骤，它确保了相同的元素相邻，从而简化了去重逻辑
 */
