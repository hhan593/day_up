# 项目1.工单系统


  2026 年上半年，项目主要围绕客服工单系统前端进行功能完善和业务适配，重点集中在 工单创建、来电建单、工单处理、任务单拉单、业务字段配置和首页数据看板 等模块。

  上半年共完成约 122 次提交，主要工作集中在 5 月和 6 月。整体变更覆盖 170 多个文件，说明项目在这半年进入了较密集的业务迭代阶段。

  ## 主要工作内容

  1. 完善工单创建和来电建单流程
     优化订单创建弹窗、字段映射、表单重置、错误处理、投诉时间处理、历史相似工单展示和工单升级能力。

  2. 推进业务字段配置化
     增强 BusinessFieldsRenderer，支持更多字段类型、级联选择、动态标签和多业务线字段配置，降低不同业务表单的维护成本。

  3. 接入和优化多业务类型
     新增或完善小兔驾考、微信券、出租车、充电、车主客、平安等业务类型表单和详情展示。

  4. 优化工单处理体验
     增加工单详情退费信息、处理表单退费字段、转发理由必填校验、直接完结能力，并调整投诉问题描述、投诉诉求等校验规则。

  5. 改进任务单和拉单能力
     优化 ETC 拉单查询、防抖逻辑、手机号限制、任务单列表展示及相关字段文案。

  6. 建设首页数据看板
     重构首页报表模块，对接首页数据接口，新增时间范围筛选、业务子页面路由、空状态处理，并优化图表和表格展示。

  7. 完善通用体验和基础能力
# 不足
  7. 1. 提交信息不够规范
     部分提交信息只有 1，也有少量提交信息带有多余符号，后续回溯问题和整理工作成果时不够直观。

  8. 业务迭代集中，重构压力较大
     5 月和 6 月改动非常密集，说明需求推进较快，但也容易带来代码耦合、重复逻辑和测试遗漏风险。

  9. 动态表单能力仍在演进中
     上半年虽然推进了 BusinessFieldsRenderer 和业务字段配置化，但多业务线表单仍频繁调整，说明配置模型可能还没有完全稳定。

  10. 首页看板经历了多轮调整
     首页从 Mock 数据、组件拆分、接口对接到空状态和筛选处理，迭代链路较长，说明前期接口格式和页面结构可能还不够明确。

  11. 环境配置变更较频繁
     .env.development、vite.config.js 多次改动，开发代理、本地 nginx、远程 API 地址来回切换，可能影响协作和环境稳定性。

  12. 部分功能有试错和回滚
     例如 GET 请求节流功能最终被还原，说明一些通用能力上线前验证还不够充分。

  13. 自动化测试体现不足
     从提交记录看，主要是功能和修复类提交，未明显看到测试补充。对于工单创建、字段映射、退费、相似工单等高频业务流程，后续可以加强测试覆盖。

  14. 文档补充偏后置
     首页数据格式文档在功能推进后才补充和迁移，说明部分复杂模块的接口约定、字段规则和业务配置说明还可以更早沉淀。
# 小兔数据小程序

  上半年主要做了什么

  1. ETC 客车与货车数据看板持续完善
      - 重构 ETC 客车收入、发行、通行等页面和接口类型。
      - 新增/调整增值服务收入总览、服务费、企业消耗订单、延保服务等页面。
      - ETC 货车模块新增收入、发行、通行相关组件，后续又做了组件结构、权限和图例展示优化。
      - 多处补充单位、千分位、趋势、TopRate、空值处理等数据展示细节。

  2. 微信券模块扩展
      - 新增保险、运营商、其他、银行等微信券数据概览组件。
      - 支持发券数量/金额、核销数量/金额等统计展示和下载。
      - 后续修复导出日期、标签页时间筛选、统计类型映射等问题。

  3. 智慧停车模块从 0 到可用
      - 5 月新增智慧停车数据看板。
      - 接入真实 API，移除 mock 数据。
      - 添加停车数据统计、临停实收趋势、车主增长、项目排名、缴费率/实收排名切换。
      - 完善日期格式、权限控制、跳转详情、趋势图弹窗、百分比精度、0 值展示等细节。

  4. ETC 助缴模块建设
      - 5 月底新增 ETC 助缴数据看板。
      - 6 月完善真实数据展示，覆盖助缴成功趋势、代收方式统计、封闭停车场助缴明细、车主排行、服务商排行、路侧订单相关统计。
      - 后续修复接口字段、跳转逻辑、业务类型、排序字段、mock 依赖和布局问题。

  5. 首页、TabBar、我的页、登录权限体系调整
      - 重构首页和我的页面，新增个人中心及登录登出能力。
      - 添加底部导航栏和自定义标签页展开面板。
      - 新增用户信息接口、权限字符支持、无权限提示和无权限图标。
      - 6 月替换权限接口为 getMenuPerms，新增 token 七日滑动续签，优化微信登录逻辑。
      - 修复权限检查不是响应式、token 缺失提示、无权限空状态等问题。

  6. 公共组件和基础能力增强
      - 新增/优化 DualStatCardPlus、EqualHeightDualCard、表格、图表、TopBar、TopTable、日期选择器等公共组件。
      - 表组件统一空值兜底显示。
      - 图表支持单位、倾斜横轴、自定义图例、空数据实例注册等。
      - 请求层支持请求级别 timeout 配置。
      - 工具函数补充数字格式化、日期处理、统计类型映射等能力。

  7. 工程配置与质量治理
      - 更新 uni-app 相关依赖。
      - 调整 Vite、TypeScript、SonarQube 配置。
      - 优化分包大小。
      - 清理废弃组件、未使用类型、旧页面和重复函数。

  过程中暴露的不足

  8. 提交规范不稳定
      - 有 featfeat、refactorrefactor、修改>问题、1、带 emoji 和不带 emoji 混用等提交信息。
      - 后续复盘、自动生成 changelog、按类型统计都会受影响。

  9. 功能上线后修复密集
      - 智慧停车和 ETC 助缴上线后，紧接着出现大量字段、接口路径、0 值误判、权限、日期格式、单位、布局问题。
      - 说明开发阶段的接口联调、边界数据验证和真机验证还不够充分。

  10. 权限和登录逻辑前期设计不够收敛
      - 上半年多次围绕权限字符、无权限展示、权限响应式、权限接口替换、token 续签、微信登录重复请求做修复。
      - 这类基础能力影响面广，说明一开始缺少统一的权限/会话设计边界。

  11. 数据展示边界问题较多
      - 多次修复 0 被显示为 --、金额单位错误、百分比精度、字段取反、长数字截断、空数据图表不注册等。
      - 这些问题适合沉淀为公共格式化函数、展示规范和组件级测试用例。

  12. mock 到真实接口切换成本偏高
      - 智慧停车和 ETC 助缴都有“新增 mock/移除 mock/修复真实接口”的过程。
      - 说明 mock 数据结构和真实接口契约之间存在偏差，建议后续用统一类型定义或接口 schema 约束。

  13. 环境切换存在提交痕迹
      - 记录里有正式环境、测试环境 API 地址切换相关提交。
      - 这类配置最好通过环境变量或构建模式控制，减少误提交和发布风险。

  14. 大文件/大范围改动较多
      - 日期选择器、智慧停车、ETC 助缴、依赖锁文件、uni_modules 等都有较大规模改动。
      - 大提交会提高 review 难度，也容易把业务逻辑、样式、配置、格式化混在一起。

  15. 自动化验证迹象不足
      - 从提交记录看，更多是人工修复 UI、字段、权限和真机白屏问题，缺少明显的测试补充提交。
      - 建议至少补齐工具函数、权限判断、数据格式化、请求层 timeout、核心看板数据转换的单元测试。
# 电子签项目
. 项目基础建设
     完成了 sjht-sign-app 小程序端项目初始化，基于 uni-app + Vue3 + TypeScript + Pinia + Wot UI + UnoCSS 搭建开发框架。补充了多环境配置、微信小程序构建脚本、分包配置、manifest 配置、静态资源、路由拦
     截、登录态存储、HTTP 请求封装等基础能力。

  2. 登录与认证体系
     实现并持续调整了登录逻辑、未登录态展示、手机号登录、企业注册、企业认证、员工认证、腾讯电子签集成认证等流程。个人信息、企业信息、企业创建人、联系电话、联系地址等管理页面也已形成基本闭环。

  3. 合同核心业务建设
     这是上半年投入最大的模块。完成了合同列表、合同详情、合同发起、合同签署、合同预览、合同下载、合同收藏、草稿箱、撤销合同、再发一次、签署邀请、分享链接、二维码下载等功能。后续又增强了企业版发起合
     同、动态签署、企业对企业签署、PDF 类型支持、文件上传、签署区域设置、坐标转换、签署截止时间、生效/到期时间校验、多位置签署等能力。

  4. 组织架构与权限管理
     完成了组织架构、部门管理、员工添加/编辑、角色新增/编辑/详情、角色同步腾讯电子签、成员管理、部门树选择、负责人切换、企业管理员身份检查等功能。权限上增加了企业管理员、企业用户权限、超管购买加量包等
     控制逻辑。

  5. 订单与套餐能力
     实现了加量包购买、下单、订单列表、订单详情、订单状态刷新、超时订单自动关闭、退款状态字段、我的加量包、协议勾选样式等功能，为商业化套餐购买流程打下基础。

  6. 首页与模板能力
     完成首页、模板列表、模板详情、模板文件下载、更多模板、使用指南、关于我们等页面；同时将应用名称从“恒通电子签”调整为“恒信签”，并补充了一批业务图标与页面图片资源。

  7. UI 与交互优化
     对合同详情、文件上传、公司注册、成员管理、员工编辑、订单列表搜索框、协议勾选组件等页面进行了多轮 UI 和交互优化。也处理了微信小程序分包路径冲突、图片路径错误、iOS 日期解析异常、PDF 页码参数、员工
     角色删除参数等实际问题。

  8. 工程质量建设
     补充了 SonarQube 配置、eslint、commitlint、husky、lint-staged、类型检查脚本等工程化工具。API 类型定义逐步完善，部分接口参数和响应类型也做了规范化调整。

  存在的不足

  9. 提交规范不稳定
     历史中存在大量提交信息为 1，也有部分提交信息带有异常 Markdown 标记，例如以 ``` 开头。这会影响后续追溯需求、定位问题和生成变更日志。

  10. 自动化测试基本缺失
     当前仓库中几乎没有业务测试文件，只发现 src/components/fg-test/fg-test.vue 这类测试/示例组件。合同签署、订单支付、认证、权限、草稿、日期处理等关键流程风险较高，但缺少单元测试、组件测试或端到端测试
     兜底。

  11. 调试代码残留较多
     src 下仍有较多 console.log、TODO 和临时调试输出，例如 App 生命周期、HTTP、locale、合同签署设置、草稿详情、角色页面等位置。这会增加生产排查噪音，也可能暴露不必要的信息。

  12. 模板遗留和示例代码未完全清理
     仓库仍保留 pages-demo、fg-test、部分模板 README、示例 ECharts 资源等内容。对于正式业务项目来说，这些内容会增加包体、干扰目录结构，也影响代码扫描结果。

  13. 工程配置仍有粗糙点
     package.json 中存在乱码描述字段，lint-staged 当前配置为简单 echo，没有真正执行 lint、format 或 type-check。说明质量门禁还没有完全落地，更多依赖人工检查。

  14. 功能推进快，但沉淀不足
     4 月到 5 月功能开发密集，合同、组织、订单、认证等模块快速成型，但从提交和代码结构看，部分能力更多是按业务页面推进，公共组件、业务 hooks、类型模型、错误处理规范还可以继续沉淀。

  15. 环境与接口配置变更频繁
     多次出现后台请求地址、测试环境、生产环境、cpolar 地址切换等提交，说明环境配置管理还不够稳定。建议后续明确环境变量规范、接口域名来源和发布前检查机制。

  16. 关键业务风险需要补强
     合同签署涉及日期、PDF、坐标、签署方、权限、认证方式、企业/个人差异等复杂逻辑。虽然上半年已修复 iOS 日期解析、PDF 页码、权限检查等问题，但这些都说明该模块边界条件较多，后续应重点补充测试用例和异常
     流程校验。

  整体来看，2026 上半年完成的是从 0 到 1 的主体功能建设：小程序基础框架、合同签署主链路、企业/员工/角色体系、套餐订单和腾讯电子签集成基本成型。不足主要集中在工程规范、测试覆盖、调试残留、模板清理和质
  量门禁落地上。
# 智能客服项目
基于当前仓库 `git log --since=2026-01-01 --until=2026-06-30`，2026 上半年能追溯到的提交集中在 **2026-06-01 到 2026-06-24**，共 **23 次提交**。当前 git 历史里没有 1-5 月的提交记录。

**上半年主要做了什么**

1. **项目初始化与基础架构搭建**
    
    - 6 月 1 日完成项目初始提交。
    - 前端基于 `Vue 3 + Vite + TypeScript + Pinia + Vue Router`。
    - 同时包含 `server/` 后端服务、Dockerfile、Nginx 配置、Socket/WebSocket 调试页面、OpenSpec 方案文档等。
    - 配置了 `pnpm`、构建脚本、类型检查、格式化脚本等基础工程能力。
2. **智能客服聊天主流程建设**
    
    - 初始版本里已经包含聊天视图、消息状态管理、聊天 API、Socket 适配层、聊天服务封装。
    - 涉及文件主要是：
        - `src/views/ChatView.vue`
        - `src/stores/chat.ts`
        - `src/services/chatApi.ts`
        - `src/services/chatService.ts`
        - `src/services/adapter.ts`
3. **智齿客服 / 后端服务集成**
    
    - 初始提交中包含 `sobot-chat-integration` 相关 OpenSpec。
    - 后端目录里有 `sobot` 模块、callback controller、token service、chat gateway 等。
    - 说明项目上半年重点之一是把前端在线客服和后端/第三方客服能力打通。
4. **Socket 通信适配**
    
    - 6 月 8 日将 `socket.io-client` 从 v4 降级到 v2，用于适配后端 `Netty-SocketIO`。
    - 重构了连接认证流程，新增 `HMAC-SHA256` 签名认证。
    - 这说明项目经历了前后端 Socket 协议兼容和鉴权方式调整。
5. **消息体验优化**
    
    - 修复了移动端首条回复展示不完整的问题。
    - 优化了媒体消息处理，包括 URL 检测、消息去重、流式渲染。
    - 清理了部分 `console.log` 调试输出。
    - 优化了转人工路径。
6. **FAQ 常见问题能力重构**
    
    - 6 月 9 日将 FAQ 从内联实现重构为独立组件和接口方案。
    - 新增/改造了：
        - `FaqBusinessList.vue`
        - `FaqCard.vue`
        - `FaqDialog.vue`
        - `FaqGroupList.vue`
        - `FaqQuestionList.vue`
        - `src/types/faq.ts`
    - FAQ 方案 ID 从 query 参数改为 route params。
    - FAQ 分页数改为从接口 `pageSize` 获取。
    - 应用信息开始写入 store。
7. **推荐相关问题功能**
    
    - 6 月 10 日新增推荐相关问题弹窗。
    - `sessionId` 从前端生成/维护调整为后端下发。
    - 这属于聊天体验增强，让机器人回答后能继续引导用户追问。
8. **页面头部与业务展示增强**
    
    - 6 月 24 日新增头部展示应用名称、头像、欢迎语。
    - 增加 `matchSource` 标签展示。
    - 说明项目从“能聊”逐步进入“更贴合业务配置和展示”的阶段。
9. **部署与配置调整**
    
    - 修改了 Nginx 配置。
    - 调整了 Vite 开发代理，指向本地后端服务。
    - Dockerfile 中加入过驾考小程序认证相关文件。
    - 后续又清理 `.env`、`.claude` 等不应跟踪的配置文件，并更新 `.gitignore`。

**过程中的不足**

1. **提交信息不规范**
    
    - 有多次提交信息是 `提交`、`1`、`feat: 1`，很难从历史判断真实意图。
    - 后续排查问题、写周报、回溯需求时成本会比较高。
2. **敏感配置曾被提交进仓库**
    
    - `.env.development`、`.env.production`、`.claude` 配置先被提交，之后又移除。
    - 还有“认证密匙”相关提交。
    - 如果这些内容是真实密钥，需要视为已泄露，建议轮换密钥，并检查远端仓库历史。
3. **锁文件管理不统一**
    
    - 仓库主要使用 `pnpm-lock.yaml`，但历史里也提交过 `package-lock.json`。
    - 这会让团队成员误用 npm/pnpm，造成依赖版本不一致。
4. **初始提交过大**
    
    - 初始提交同时包含前端、后端、OpenSpec、工具配置、文档、截图、环境示例等。
    - 这会降低代码审查质量，也不利于后续定位“某个能力是从哪次改动引入的”。
5. **README 仍偏模板化**
    
    - 当前 README 主要还是 Vite 模板说明，没有充分描述项目业务、启动方式、环境变量、部署方式、接口依赖、Socket 鉴权流程等。
    - 对新成员接手不够友好。
6. **临时改动进入主线**
    
    - 有“临时：隐藏文件发送功能入口”这样的提交。
    - 说明功能开关或发布策略还不够完善，建议后续用配置开关、灰度开关或权限控制承载这类临时需求。
7. **测试记录不明显**
    
    - 从提交历史看，主要是功能和修复提交，没有看到针对 FAQ、Socket 连接、消息流式渲染、移动端展示等核心能力的测试补充。
    - 对聊天类项目来说，这些地方很容易出现回归。
8. **文档有迁移和清理痕迹，但体系还需收敛**
    
    - 6 月 9 日新增了多份 FAQ/推荐问题设计文档，随后又清理旧版技术方案和需求文档。
    - 说明文档在快速演进，但需要明确哪些是现行方案、哪些是历史方案，避免团队理解不一致。

整体看，2026 上半年这个项目主要完成了从初始化到“智能客服聊天可用”的核心建设，并围绕 Socket 兼容、鉴权、FAQ、推荐问题、移动端体验和业务配置展示做了集中迭代。短板主要集中在工程规范、提交质量、敏感信息管理、测试覆盖和文档沉淀上。
